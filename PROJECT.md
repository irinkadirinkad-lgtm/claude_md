# Проекты — стабильные факты

Один файл на все действующие проекты (не по файлу на проект) — раздел `##` на каждый.
Живой статус/TODO по каждому проекту — в `STATUS.md` его собственного репозитория, не здесь.

## Yclients Finance App

Репозиторий: `Yclients_integration_tables_application` (main). Автоматизация финансовых
операций салона и магазина — форма/журнал → YClients, Планировщик расходов,
импорт сотрудников, приложение-просмотрщик. Живой статус/ход работы — в `STATUS.md`
того репозитория, не здесь.


### Текущее состояние — готово и работает

#### Google Таблица «Финансовые операции — рабочая таблица»
https://docs.google.com/spreadsheets/d/1gt3dtbbx-JigKtlPA9AdicRNewMDy29M86xaFSvgbaU/edit
- Лист 1 (журнал): Дата, Поставщик, ID поставщика, Статья платежа, ID статьи платежа, Касса, ID кассы, Комментарий, Номер телефона клиента, Сумма ₽, Отправлено в YClients
- Лист «Справочники»: Тип, Название, ID

#### Форма ввода — Google Apps Script Web App
Ссылка (production, версия v62): https://script.google.com/macros/s/AKfycbwb4Xb2u8WNO9bHOes1MihWH8FeI5jExClurhtVQpkpSVgP-Yw2anyVuow6upo-39w/exec
Это ОТДЕЛЬНЫЙ standalone-скрипт (scriptId в `.clasp.json` этого репозитория), НЕ привязан
к конкретной таблице — обращается к нескольким таблицам через `SpreadsheetApp.openById()`.
Живёт в этом репозитории (`Код.js`, `index.html`, `netlify/index.html`, `appsscript.json`).

**Важно: этот скрипт делает больше, чем просто форму ввода** — см. «Планировщик» ниже.
Так исторически сложилось (правки делались напрямую в редакторе Apps Script, минуя git) —
теперь синхронизировано (`git log`, коммит «Sync repo with live Apps Script project»).

Файлы:
- `Код.js`: справочники (поставщики/статьи/кассы) + getReferenceData/addReferenceItem/addEntry,
  sendToYClients (ежедневная отправка журнала в YClients), и весь блок «Планировщик» (см. ниже).
- `index.html` / `netlify/index.html`: розовая форма — Дата/время, Поставщик, Статья, Касса
  (у каждого «➕ Добавить новое…»), Комментарий, Сумма.

#### Доступы к YClients
Хранятся в Apps Script → Project Settings → Script Properties (НЕ в коде, НЕ в репозитории):
PARTNER_TOKEN, COMPANY_ID=328251, YC_LOGIN, YC_PASSWORD, USER_TOKEN.
Технический пользователь YClients создан специально для интеграции (не личный аккаунт).

#### Справочники — сверены с YClients
- Статьи платежа: 39 пунктов с реальными ID
- Кассы: 6 пунктов, ID подтверждены
- Контрагенты: список подтверждён

#### API YClients — подтверждено диагностикой
- `POST /api/v1/finance_transactions/{company_id}` — создание операции (работает).
  Поля: expense_id, amount, account_id, client_id, supplier_id, master_id, comment, date ("YYYY-MM-DD HH:MM:SS")
- `GET /api/v1/finance_transactions/{company_id}` — **405 Method Not Allowed**, читать через
  этот эндпоинт нельзя (только создание).
- Авторизация: Authorization: Bearer {partner_token}, User {user_token}
- user_token: POST /api/v1/auth {login, password}
- `GET /api/v1/staff/{company_id}` — 200 OK, список сотрудников (id, name, specialization,
  position{id,title}, fired, status).
- `GET /api/v1/storages/{company_id}` — 200 OK, список складов. У салона их 2: id 637822
  «Основной склад» (for_service — расходники на услуги), id 637823 «Магазин»
  (for_sale — розница/косметика).
- `GET /api/v1/records/{company_id}` — **200 OK** (после того как Ирина расширила права
  техническому пользователю 04.07.2026). Фильтры: `start_date`, `end_date`, `count`. Ключевые
  поля записи: `staff_id` (кто оказал услугу), `created_user_id` (кто оформил визит —
  например, id администратора Элины) — то самое разделение «мастер обслужил» vs
  «администратор записал», которое нужно было для Ввода. `services[]` (title, cost) —
  выручка услуг. `goods_transactions[]` (title, cost, master_id, storage_id, good_id) —
  товары, проданные В РАМКАХ визита. `attendance`/`visit_attendance` — статус посещения
  (1 = пришёл).
- **ВАЖНО про «новый клиент»:** `client.is_new` и `client.success_visits_count` внутри
  записи — это ТЕКУЩЕЕ состояние клиента на момент запроса API, а НЕ историческое состояние
  на момент того визита. Проверено на реальных данных (04.07.2026): у клиента с несколькими
  визитами за 4 месяца `success_visits_count` ОДИНАКОВЫЙ на всех его визитах (например,
  везде «52») — это просто «сколько визитов у него сейчас», не история. Значит: **нельзя
  брать эти поля из старой записи и считать, что они отражают состояние клиента в дату
  записи.** Единственный надёжный способ определить «это был первый визит клиента» —
  считать самим: найти дату самого раннего визита клиента по ВСЕЙ его истории (не только
  за целевой период) и сравнить с датой конкретной записи. Иначе бэкфилл старых месяцев
  задним числом будет давать неверные цифры по новым клиентам.
- `GET /api/v1/schedule/{company_id}/{staff_id}/{start_date}/{end_date}` — **200 OK** (пути
  через `/`, не `?query`). Отдаёт массив по дням: `{date, is_working, slots:[{from,to}]}` —
  это и есть настоящие «Смены», не привязанные к визитам (ровно то, что просила Ирина).
- **Розничные продажи БЕЗ визита — РЕШЕНО, полностью читается задним числом.** Не через
  `records` (там их нет — проверено за 4 месяца, 0 совпадений) и не через `storage_operations`/
  `goods_operations`/`documents` (это всё эндпоинты СОЗДАНИЯ, не чтения — 404 или 400 на
  чтение). Правильный путь нашёлся в официальной документации YClients (не в JS-версии сайта,
  которую я не могла прочитать, а в разделе API-документации, куда Ирина зашла напрямую):
  - **`GET /api/v1/transactions/{company_id}`** — НЕ `finance_transactions` (это другой,
    create-only ресурс). Фильтры: `master_id`, `start_date`, `end_date`, `client_id`,
    `account_id`, `type`, `page`, `count` и др. Возвращает ВСЕ финансовые операции сотрудника
    за период — включая продажи товара БЕЗ визита (`record_id:0, visit_id:0`,
    `expense:{id:7, title:"Продажа товаров"}`) и, что важно, **выручку по услугам тоже**
    (`expense:{id:5, title:"Оказание услуг"}`) — с привязкой к `master_id` в обоих случаях.
    Также встречаются служебные записи типа зарплатных выплат (`expense.id:3`,
    отрицательная сумма) — их отфильтровывать, брать только `expense.id` 5 и 7.
  - Проверено на реальных данных (04.07.2026): по мастеру Луизе за тестовый период отдельно
    просуммировались услуги (`id:5`) и товар (`id:7`), суммы разумные.
  - **Вывод: `transactions` — единственный нужный источник выручки** (и услуг, и товара,
    визит был или нет). Парсить `goods_transactions[]` внутри `records` для товара больше
    не требуется — всё покрывает `transactions`. `records` остаётся нужен только для
    подсчёта визитов/клиентов/нового клиента и разделения мастер (`staff_id`) vs
    администратор (`created_user_id`).
  - Вебхуки (`hooks_settings`) для этой задачи больше НЕ нужны — реальное время не
    требуется, `transactions` и так даёт полную историю назад (можно бэкфилить март-май).

#### Планировщик расходов — теперь ЧАСТЬ этой цепочки (не «отдельно», как было раньше)
Google-таблица «Планировщик расходов» (id `12zlcXPzG_h4pmkHFlOU_PU7p7Rrk3tTtu_qYOZGLHpg`),
листы «Доходы и расходы» и «История». Автоматизация в `Код.js` (в этом же скрипте, что и форма):
- `pushNewEntriesToPlanner_` / `updatePlanner` — новые строки журнала прибавляются к
  соответствующим статьям в «Доходы и расходы» (колонка C), без дублей (отметка в журнале, кол. L).
- `closeMonthAndUpdate` — закрытие месяца: архивирует прошедший месяц в «Историю»
  (`archiveMonthToHistory_`), пересчитывает текущий месяц с нуля из журнала
  (`resetAndRecomputeCurrentMonth_`), с самопроверкой (если архив не подтвердился — ошибка + email).
- Email об ошибках на irinkadirinkad@gmail.com (`sendErrorEmail_`).
- Триггеры (`setupAllTriggers`): sendToYClients ежедневно в 21:00; updatePlanner каждое
  воскресенье в 22:00; closeMonthAndUpdate 1, 2 и 3 числа месяца в 01:00 (повтор при сбое).

**Осторожно:** `setupAllTriggers()` при вызове удаляет ВСЕ существующие триггеры проекта и
создаёт заново только эти пять. Если для нового функционала понадобится свой триггер —
не создавать через отдельный вызов `deleteTrigger`, либо добавлять его прямо в этот список.

#### Доступ к Apps Script этого проекта (clasp)
Ирина согласовала: Claude может сам авторизоваться через `clasp` и напрямую пушить код
в Apps Script проект «Финансовые операции», а также читать логи выполнения — без
промежуточной пересылки файлов/скриншотов туда-обратно. Общий механизм (токен не переживает
сессию, нужен повторный логин) — см. CLAUDE.md, раздел про доступ к внешним инструментам.

**Более быстрый способ авторизации clasp (обнаружено 04.07.2026, из ветки main):** вместо
интерактивного `clasp login` каждый раз, можно прочитать уже готовый `~/.clasprc.json` из
Google Drive — файл «CLAUD системные/clasp-credentials.json» (id
`121GGUUB7g8TFzGfkoCQwnefA0xdW_Swv`), записать его содержимое в `~/.clasprc.json` на диске,
и `clasp` сразу заработает без ссылки-авторизации через Ирину. Проверить, что токен внутри
ещё не протух (refresh_token обычно долгоживущий) — если протух, тогда уже обычный
интерактивный логин.

**Production deployment ID** (веб-приложение, `AKfycbwb4Xb2u8WNO9bHOes1MihWH8FeI5jExClurhtVQpkpSVgP-Yw2anyVuow6upo-39w`)
— его нужно обновлять (`clasp deploy --deploymentId <ID>` или через Apps Script API) после
пуша, если изменения должны увидеть реальные пользователи формы (не просто HEAD/тестовый
деплой).

**Автодеплой через GitHub Actions:** `.github/workflows/deploy-apps-script.yml` — при пуше
в `main`/`master` автоматически делает `clasp push --force` в этот же scriptId. Требует
секрет `CLASPRC_JSON` в GitHub → Settings → Secrets (содержимое — то же
clasp-credentials.json из Drive). Значит: пуш в `main` — не просто «сохранить в git», а
реальный деплой в продакшен. Осторожно с тем, что сливаешь в `main`.

### Блок 2, часть 2 — Импорт сотрудников из YClients в «Ввод»

Отдельный Apps Script проект (изоляция от «Финансовые операции» выше, см. CLAUDE.md).
Живёт в подпапке `employees-integration/` этого репозитория.

#### Целевой файл
Google-таблица «Сотрудники финансы.xlsx», id `1A1efYis0odAtZHh-hu10shBNf8xRIsnL`.
Листы: Dashboard, Динамика по месяцам, Ввод, Импорт, Свод, Динамика по сотруднику,
Мастера маникюра/Парикмахеры/Администраторы (аналитика), Настройки.
Справочник сотрудников — НЕ отдельным листом, а внутри «Настройки», строка заголовка 67
(«Сотрудник (ключ)», Имя, Направление, Тип оплаты, Своя ставка?, % от услуг, Ставка за
смену, % с продаж, Статус). Активные сотрудники сейчас: Луиза — Мастер маникюра,
Ирина — Арт-Директор, Элина — Администратор, Ирина — Администратор (двойная роль,
см. правило ниже).

Лист «Ввод» — данные с строки 6, колонки A-I: Месяц, Сотрудник (ключ), Направление,
Тип оплаты, Смены, Клиенты, Новые клиенты, Выручка услуг, Продажи товара. Остальное
(J и правее) — формулы, не трогаем при записи.

**Двойная роль сотрудника** (пример — Ирина, Арт-Директор + Администратор): ДВЕ отдельные
строки в «Ввод», не пытаться разделить одного человека автоматически по смыслу:
- Как мастер — визиты/выручка/товар по `staff_id` (кто оказал услугу).
- Как администратор — клиенты/новые клиенты/выручка услуг по `created_user_id` (кого
  записала, независимо от того, кто обслуживал). Смены — те же, что и в строке мастера.
- Правило универсальное (`staff_id` vs `created_user_id`), не только для Ирины.
- Товар: и мастер, и администратор МОГУТ иметь личные продажи — считать по каждому
  уникальному id сотрудника в YClients РОВНО ОДИН раз за месяц (не по роли), иначе у
  двухролевого сотрудника (один id под двумя строками в «Настройки») товар задвоится.

**Продажи без сотрудника:** продажи Ирины (в любой роли) идут не в её личную строку, а в
отдельную служебную строку «Без сотрудника — продажи» в «Ввод» (статистика салона в целом,
без искажения её собственных показателей — % с продаж она не получает).

#### Apps Script проект
`scriptId` (в `.clasp.json` этой подпапки): `1LrQhOvbjIRpfjf8m20kBixp1XJLDT85EL3nNXjY5rO-enzjaDwmNyaN8`
Деплой (веб-приложение): `https://script.google.com/macros/s/AKfycbw0dSZa_PUSRet0PMgtJJCep811_BT6-CdivnclafaeqNGyrdQTrUicQReIqPmT0xy_Dg/exec`
Скрипт Properties (свои, в этом проекте, НЕ шарятся с «Финансовые операции»):
PARTNER_TOKEN, COMPANY_ID=328251, YC_LOGIN, YC_PASSWORD, USER_TOKEN (обновляется сам).

**Доступ изменён 05.07.2026:** `webapp.access` был `MYSELF` (только по логину), стал
`ANYONE_ANONYMOUS` — понадобилось для кнопки «Обновить данные» в просмотрщике (Блок 3),
которая дёргает `runMonth` прямо со статичного сайта, без возможности передать логин.
Осознанный компромисс: этот эндпоинт теперь может писать в «Ввод» и дёргать YClients API
без авторизации, если кто-то узнает ссылку — риск невысокий, т.к. ссылка нигде не
публикуется (только домашний экран Ирины), но при инциденте (утечка ссылки, подозрительные
записи в «Ввод») первым делом проверить это место.

Старый `scriptId` `1EF8J3JCPxVSKjLdjrUC_dFtUbfxXeZpkhauzRieePgVfkFy3zu2eNCwr` — привязан
к УДАЛЁННОМУ дублирующему файлу («...до.xlsx»), с которым по ошибке начиналась работа.
Заброшен, действий не требует.

#### Как это работает
Лист «Импорт»: ячейка C5 — месяц (первое число), C6 — чекбокс «Загрузить из YClients»,
C8 — статус последней загрузки. Чекбокс → `onEdit`-триггер (`onImportCheckboxEdit_`,
установлен через `setupImportTrigger()`) → `runImport()` → пишет в «Ввод», обновляя
существующую строку месяц+сотрудник (не задваивая) или добавляя новую.
Redeploy-скрипт для этой сессии: `bash <scratchpad>/redeploy_new.sh` (push + новая версия +
обновление деплоя), путь скрипта пересоздаётся заново в новых сессиях при необходимости.

#### Деплой формы на Netlify
Форма (`netlify/index.html`) также задеплоена на Netlify: https://finance-el-naturel.netlify.app
Токен — в Google Drive рядом с clasp-credentials: id `16g_9WnZyOMjqHJyjXqGae02VKxMMSq4k`.
Деплой: `npm install -g netlify-cli`, затем
`NETLIFY_AUTH_TOKEN=<токен> netlify deploy --site 43fb3e56-ee06-495a-a308-611f9fb87077 --dir netlify --prod`.
Netlify MCP-инструменты (если подключены) требуют подтверждения на каждый вызов — проще
через CLI с токеном.


### Блок 3 — приложение-просмотрщик

#### Архитектура (согласовано с Ириной 04.07.2026)
- **Бэкенд** — отдельный, ТОЛЬКО ЧИТАЮЩИЙ Apps Script проект `viewer-api/` (изоляция от
  `employees-integration/`, где есть функции записи — так безопасно открыть без логина).
  `scriptId` (в `.clasp.json` этой подпапки): `1nmUl22Iyns4kMK4NuzB7knzx-9Ly8DVqjpNzDs15cZUmZffPQAD2A4DD`.
  Standalone-скрипт (не привязан к таблице), обращается к «Сотрудники финансы.xlsx» через
  `SpreadsheetApp.openById()`. `appsscript.json`: `webapp.access = ANYONE_ANONYMOUS`,
  `executeAs = USER_DEPLOYING` — то есть выполняется от имени Ирины (у нее есть доступ к
  таблице), а не от имени того, кто открыл ссылку; сама таблица никому не открыта.
  Деплой (веб-приложение): `https://script.google.com/macros/s/AKfycbw5p5_kYK9dnFzMN_fBo0RgPUAtLTfxiKMEATa0R2CiRR68ZbCQ96dNtSL5USkEdAKW/exec`
  Действия: `?action=dashboardSvod&month=YYYY-MM` (данные для «Свод»), `?action=monthOptions`
  (список месяцев для выпадающего списка, тот же справочник, что на листе «Свод»,
  `Dashboard!Q2:Q49`/`R2:R49`).
  **Важно (разово для новых деплоев этого проекта):** новый Apps Script проект должен быть
  один раз подтверждён владельцем (Ирина открывает `.../exec` в браузере, залогинившись,
  проходит экран «Google не проверял это приложение» → Дополнительно → Перейти → Разрешить) —
  иначе даже анонимные посетители получают отказ. Разовое действие, не при каждом заходе.
- **Логика сборки данных** — НЕ пересчитывает формулы «Свод» и не трогает выпадающий список
  месяца на этом листе (иначе лист дёргался бы на другой месяц у Ирины, если она смотрит его
  в этот момент). Вместо этого читает уже посчитанные строки листа «Ввод» за нужный месяц
  (там уже готовы Себестоимость/Итоговая ЗП/Чистая прибыль на сотрудника) и агрегирует сама:
  - Оборот компании = сумма (Выручка услуг + Продажи товара) по направлениям, НЕ считая
    администраторов (их «выручка в смены» — это выручка МАСТЕРОВ, для оценки эффективности
    админа, задвоила бы оборот) + личные продажи товара администраторов + «Без сотрудника».
  - Остальные компанейские итоги (ЗП/себестоимость/прибыль) — простая сумма соответствующих
    колонок «Ввод» по всем строкам месяца (эти колонки уже учитывают все нюансы построчно).
  - «Целевая выручка в месяц» и список направлений — читаются из «Настройки» поиском по
    тексту заголовка (не по номеру строки, чтобы не сломаться при правках таблицы). Важный
    нюанс: в реальных ячейках заголовков — перенос строки (`\n`), не пробел («Дата\n(действует
    с)») — сравнение текста нормализует пробелы (`replace(/\s+/g,' ')`), иначе поиск не находит.
  - Сверено на реальных данных за март 2026 — сошлось с Dashboard день в день (оборот 42 990 ₽,
    прибыль −9 473 ₽, ЗП 44 014 ₽, % от цели 11%, запас/дефицит нормы админов −194 200 ₽ /
    −169 400 ₽, не хватает клиентов 57 / 27).
- **Фронтенд** — статичная страница `viewer/index.html`, задеплоена на Netlify как ОТДЕЛЬНЫЙ
  сайт (не в тот же проект, что форма): **https://elnaturel.netlify.app**
  (site id `3a12eb19-d320-4468-93a0-36afc6287753`, тот же Netlify-токен, что и у формы, — см.
  выше; сайт переименован дважды: `elnaturel-svod` → `elnaturel-financi` → `elnaturel`,
  05.07.2026, старые имена больше не работают).
  PWA: `viewer/manifest.webmanifest` + `viewer/icon-192.png`/`icon-512.png` — при добавлении
  на домашний экран телефона (Safari/Chrome → «На экран Домой») открывается как отдельное
  приложение, без адресной строки браузера и вкладок Google — то, что просила Ирина.

  **Визуальный стиль — этот раздел ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ (весь CSS живёт в `<style>` внутри
  `viewer/index.html`, строки ~26-406 на 05.07.2026). Утверждён Ириной, использовать для всех
  новых разделов без нового согласования макета — только состав показателей уточнять.**

  - **Цвет.** Фирменный `--brand: #D8AAAA` (CMYK 15-36-25-0, прислан Ириной как эталон,
    ОДИНАКОВ в обеих темах) — используется в пятнах фона (`body::before`, радиальные градиенты)
    и фоне иконки/пароля. Акцентный `--rose-900` (текст/иконки/кольца/бренд-плашка) МЕНЯЕТСЯ по
    теме: `#8C4258` светлая / `#E58BA3` тёмная. Остальные токены: `--good`/`--bad` (зелёный/
    красный по знаку числа), `--chart-blue` (выручка на графиках), `--dir-1..5` (категориальная
    палитра для линий на графике по направлениям — фиолетовый/оранжевый/розовый/жёлтый/бирюзовый
    в этом порядке; порядок и цвета подобраны через `dataviz`-skill, специально избегая
    blue/green/red, т.к. те уже заняты под выручку/прибыль), `--ink`/`--ink-soft`/`--ink-faint`
    (текст по убыванию контраста), `--page-bg`/`--card-bg`/`--line`/`--line-strong` (фон/рамки).
    Сами плашки/карточки — **бесцветное стекло**: заливка нейтральная белая
    (`--tile-fill-1/2`), цвет виден только там, где сквозь `backdrop-filter` просвечивают пятна
    фона — НЕ подмешивать розовый в заливку самой плашки, отклонено явно и обсуждалось не раз.
    **Важная ловушка при правке любого цвета:** значения продублированы в ЧЕТЫРЁХ местах —
    `:root` (дефолт), `@media (prefers-color-scheme: dark)`, `:root[data-theme="dark"]`,
    `:root[data-theme="light"]` (два последних — для ручного переключателя темы, см. ниже) —
    забыть один из четырёх легко, всегда проверять все четыре после правки палитры.
  - **Эффект стекла** — три обязательных компонента, без них не «читается» как стекло:
    1. Сильный `backdrop-filter: blur(34px) saturate(180%)` (не меньше).
    2. Направленный контур: светлый верх/лево, тёмный низ/право (`--glass-border-top/bottom/
       right`) — имитирует грань стекла, ловящую свет с одной стороны.
    3. Многослойная тень (`--glass-shadow`): внешняя мягкая + `inset` светлая сверху + `inset`
       тёмная снизу, для ощущения толщины. Никаких глянцевых бликов-пятен — пробовали, Ирина
       отклонила («как глянцевый шар, не стекло»).
    Компоненты, использующие этот рецепт: `.tile`, `.direction-card` (карточки направлений,
    администраторов на «Направления», И карточки мастеров/админов на «Сотрудники» — один общий
    класс на все контексты), `.chart-card`, `.gate-card` (экран пароля).
  - **Типографика — ОДИН системный sans везде** (`--sans`), включая заголовки `h1`/`h2` —
    ранее там стоял Georgia/serif, убрали по прямой просьбе Ирины («шрифт должен быть в одном
    стиле, различия — только по толщине/размеру»). `--serif` (Georgia) в токенах остался
    объявлен, но нигде не используется — можно удалить при следующей уборке, если не
    понадобится. Иерархия текста — только size/weight/color, не typeface: `.tile-label`/
    `.ticker-label` ~10.5px uppercase `--ink-soft` 600, `.tile-value` ~21px 700, `.ticker-value`
    ~16.5px 700 (оба ticker-размера увеличены с 9.5px/15px по просьбе Ирины — «контрастнее и
    крупнее»), `.direction-turnover` ~22px 700.
  - **Компоновка карточки** (везде — направления/админы/мастера): заголовок с точкой-маркером
    → главное число крупно → строка-дельта (например «5 клиентов · 11 смен · 1 новый клиент» —
    ВКЛЮЧАЕТ новых клиентов, добавлено по фидбеку) → горизонтальная лента `.ticker`
    (`overflow-x: auto`, ОБЯЗАТЕЛЬНО `flex: 0 0 auto` у пунктов, иначе сжимаются вместо
    прокрутки). Есть компактный вариант `.ticker.compact` (мельче отступы/шрифт — для плотных
    списков вроде «Динамика по месяцам» в «По направлениям») и двухстрочное значение пункта
    (`.ticker-value-sub` — вторая строка помельче, используется в «Расчёт KPI по месяцам»:
    сверху KPI, снизу новые клиенты).
  - **`statsTile()`** (JS-хелпер) — широкая плашка, объединяющая Клиенты/Смены/Новые клиенты
    в ОДНУ строку с тремя мини-парами метка/число вместо трёх отдельных квадратных плашек.
    Стоит первой в блоке показателей месяца/года. Общее правило сетки плашек (`.tiles`,
    2 колонки): число НЕ-широких плашек в блоке должно быть чётным, иначе последняя виснет
    одна в пустой строке — если после правок получилось нечётное число, либо добавить ещё один
    показатель, либо сделать лишний широким (`wide`).
  - **Кольцо-индикатор** (`ringSvg()`/`ringsRow()` в JS) — SVG-круг, обводка `--rose-900`
    (единственное место акцентного тона на самой плашке, не на фоне) — для «доли от общего»
    (% от оборота/клиентов/продаж «в группе» — что для мастеров, что для админов). Используется
    ТОЛЬКО в «хero»-разделах (выбранный месяц, годовые итоги), НЕ в компактных списках
    по месяцам — там те же метрики остаются текстом в `.ticker` (не хватает места на кольца).
  - **Круговая диаграмма** (`pieSvg(title, segments, opts)` в JS) — в отличие от `ringSvg`,
    ПОЛНОСТЬЮ ЗАЛИТЫЙ круг (не контур): каждый сегмент — доля одного сотрудника от суммы
    показателя по всей группе, цвета `--dir-1..5` по кругу, легенда снизу (цвет · имя · %).
    ВСЕГДА парой (`.pie-row`, размер 130) — выручка + клиенты рядом, никогда одна диаграмма
    в одиночку (одна на всю ширину смотрится куцо). Заголовок — `opts.subtitle` даёт вторую
    строку ПОД основным названием показателя (не через конкатенацию строкой типа «Выручка —
    Направление» — из-за разной длины текста верхняя строка переносится по-разному у двух
    соседних карточек, и круги визуально «плывут» по высоте; раздельные строки держат все
    карточки одной высоты). Три места использования: (1) «Сотрудники → Мастера» — под строкой
    месяца, подпись «Выручка»/«Клиенты» + подзаголовок направления ВЫБРАННОГО мастера, доля
    среди ВСЕХ мастеров того же направления (не только выбранный — сравнение всех сразу); (2)
    «Сотрудники → Администраторы» — то же самое, подзаголовок «Администраторы», по выручке в
    смены + клиентам среди всех админов (дополнительно к кольцам % у ВЫБРАННОГО админа — не
    замена, отдельный разрез); (3) «По направлениям» — перед списком карточек, без подзаголовка
    (направление и так видно в селекторе над диаграммами) — выручка и клиенты среди сотрудников
    текущего направления (для «Администраторы» — выручка в смены вместо выручки услуг).
  - **Графики** — ручной SVG, без библиотек. `chartSvg(months, {valueKey, valueLabel})` —
    сгруппированные столбики (выручка + прибыль/убыток по знаку), параметризован, переиспользуется
    и для компании, и для мастера/админа. `directionsChartSvg(months, directions)` — ЛИНЕЙНЫЙ
    (не столбчатый — меняли по фидбеку) график на «Направления», по линии на направление,
    цвета из `--dir-1..5`; администраторы туда НЕ включаются (пробовали — Ирина попросила
    убрать, это отдельный от направлений график).
  - **Синхронная прокрутка** (`syncTickerScroll(container)`) — общий `scrollLeft` у всех
    `.ticker` внутри одного контейнера. На «По направлениям» — ДВЕ независимые группы на одной
    странице (снимок месяца `.direction-snapshot` и «Динамика по месяцам» `.monthly-breakdown`),
    не связаны между собой намеренно (разный набор полей/контекст).
  - **Ручное переключение темы** — два кружка (`.theme-toggle`, обычный поток страницы, В
    САМОМ НИЗУ, НЕ зафиксировано при скролле — Ирина явно просила не прибивать к экрану).
    Хранится в `localStorage` + `cookie` (год жизни, резерв — iOS может чистить localStorage
    у PWA с «Домой»). Применяется через `document.documentElement.dataset.theme`, что и требует
    третьего/четвёртого блока цветовых токенов (`[data-theme="dark"]`/`[data-theme="light"]`)
    поверх системного `prefers-color-scheme`.
  - **Логотип EL'NATUREL** (`viewer/logo-mark-light.png` розовый / `logo-mark-dark.png`
    БЕЛЫЙ — не розовый, Ирина явно попросила поменять) — эмблема (круг+тюльпан), вырезанная
    из присланных Ириной файлов `ELNATUREL_LOGO4/5`, показывается в шапке справа
    (`.header-row`/`.header-logo`, переключение по теме через CSS) и на экране пароля.
    Иконка на «Домой» (`icon-192.png`/`icon-512.png`) — фирменный розовый фон, БЕЛАЯ эмблема,
    МАТЕМАТИЧЕСКИ (не оптически) равные отступы со всех 4 сторон — Ирина явно уточнила именно
    это после первой версии с оптической правкой.
  - **Экран пароля** (`#gate` в `viewer/index.html`) — тот же стеклянный рецепт карточки
    (`.gate-card`), SHA-256 хеш пароля в JS, сравнение на клиенте, флаг в `localStorage`.
    Осознанно слабая защита («не пустить случайного человека со ссылкой», не от взлома) —
    решение Ирины, платную защиту Netlify не подключать.
  - Кнопка «Обновить» — рядом с выбором месяца на «Направления», дёргает
    `employees-integration` (`?action=runMonth&month=YYYY-MM`, тоже открыт `ANYONE_ANONYMOUS` —
    см. ниже) и после успеха перезагружает дашборд с тем же месяцем.
- **Этап 1 (готово, утверждено Ириной 05.07.2026)** — раздел «Свод»: компания за месяц (оборот,
  чистая прибыль, общая ЗП, % от цели, клиентов за месяц, активных сотрудников, продажи без
  сотрудника — одной широкой плашкой), лента себестоимость/эквайринг/налог; карточки по
  направлениям (главное число — чистая прибыль, лента остальных); карточки по администраторам
  (выручка в смены + кольцо % от общей выручки админов, лента остальных показателей). Смотрит
  только Ирина, доступ не ограничен паролем (ссылка не публикуется).
- **Этап 2 (готово, утверждено Ириной 05.07.2026)** — вкладка «Сотрудники» (аналог листа
  «Динамика по сотруднику»), сначала только мастера, затем (этап 3) — переключатель на
  администраторов и направления внутри той же вкладки.
  - Выбор сотрудника + месяца → карточка выбранного месяца (первая плашка широкая — выручка
    услуг, под ней чистая прибыль, затем объединённая плашка Клиенты/Смены/Новые клиенты —
    первой в блоке), лента остальных показателей месяца (в т.ч. «не хватает клиентов», % от
    оборота/клиентов/продаж — кольцами, только по месяцам, не в годовых итогах).
  - Годовые итоги — сумма по всем месяцам года (включая себестоимость, средний чек, бонус с
    продаж и «не хватает клиентов», без процентов — они бессмысленны в сумме).
  - Компактный SVG-график (`chartSvg()` в JS, без внешних библиотек) — сгруппированные столбики
    по месяцам, синий = выручка услуг, зелёный/красный (по знаку) = чистая прибыль, общая
    нулевая базовая линия, текстовая легенда под графиком.
  - «Сравнение мастеров» внизу вкладки — два независимых выпадающих списка (направление →
    метрика), НЕ общий список всех мастеров вперемешку. Данные (`getMastersComparison_`)
    кэшируются на фронте один раз при открытии вкладки, фильтрация только клиентская.
  - Ручное переключение темы (независимо от системной) — два кружка внизу страницы (`.theme-toggle`),
    сохранение выбора в `localStorage` + `cookie` (год жизни, резерв — на iOS PWA localStorage
    ненадёжен), применяется через `document.documentElement.dataset.theme` (override к
    `prefers-color-scheme` в обе стороны). Явно НЕ зафиксировано при скролле — обычный элемент
    в потоке страницы, в самом низу после `<footer>`.

- **Этап 3 (готово, утверждено Ириной 05.07.2026)** — администраторы, «По направлениям», логотип,
  пароль. Подробный ход работы — в STATUS.md, здесь только устоявшиеся факты:
  - Вкладка «Сотрудники» теперь имеет три переключателя: «Мастера» / «Администраторы» /
    «По направлениям». Админские показатели ОТЛИЧАЮТСЯ от мастерских (нет себестоимости/
    эквайринга/налога — уже учтены на уровне мастеров; есть KPI, «Запас/дефицит нормы»,
    «% от оборота/клиентов среди админов») — не путать наборы полей при доработках
    (`getAdminDynamics_`/`getAdminsComparison_` в `viewer-api/Код.js` отдельные от мастерских
    `getEmployeeDynamics_`/`getMastersComparison_`).
  - «По направлениям» — выбор направления (из реального списка «Настройки» + добавленный
    вручную во фронтенде пункт «Администраторы») + месяц/год → все сотрудники направления
    рядом, с синхронной горизонтальной прокруткой показателей (`syncTickerScroll()` — общий
    `scrollLeft` у всех `.ticker` внутри одного контейнера). Два независимых контейнера
    синхронизации на странице: снимок месяца (`.direction-snapshot`) и «Динамика по месяцам»
    (`.monthly-breakdown`) — не связаны между собой намеренно (разный набор полей).
  - «Средний чек» (колонка I) и «Бонус с продаж» (колонка Q, индексы 9 и 16 в `Ввод`,
    0-based) — уже существующие формулы листа «Ввод», просто раньше не читались бэкендом.
    Теперь `readVvodRowsForMonth_`, `getEmployeeDynamics_`, `getAdminDynamics_` читают оба поля.
    Компанейский «Средний чек» на «Направления» — НЕ из этой колонки (та — на сотрудника), а
    отдельно вычисляется как `оборот / клиентов` в `getSvodDashboard_`.
  - Первая вкладка переименована «Свод» → «Направления» (раздел внутри неё называется
    «Свод по направлениям» — сохранили как заголовок раздела).
  - Логотип EL'NATUREL: `viewer/logo-mark-light.png` (розовый, светлая тема) и
    `viewer/logo-mark-dark.png` (белый — специально не розовый, тёмная тема; исходники обоих
    — обтравленная эмблема из присланных Ириной файлов `ELNATUREL_LOGO4/5`, тонированная
    через PIL). Стоит в шапке справа (`.header-row`/`.header-logo`, переключение по теме
    через CSS) и на экране пароля. Иконка `icon-192.png`/`icon-512.png` — фирменный
    розовый фон, БЕЛАЯ эмблема, математически равные отступы со всех 4 сторон (не оптический
    центр — Ирина явно попросила именно математический).
  - Пароль при входе (`#gate` в `viewer/index.html`) — SHA-256 хеш пароля зашит в JS
    (`PASSWORD_HASH`), сравнение на клиенте, флаг `gatePassed` в `localStorage`. Осознанно
    слабая защита (клиентский JS можно обойти просмотром исходного кода) — задача не
    «остановить взлом», а «не пустить случайного человека с утёкшей ссылкой», этого достаточно
    по решению Ирины. Пароль сообщён только Ирине, в репозиторий как открытый текст не
    коммитить (только хеш).
  - Сайт переименован `elnaturel-financi` → `elnaturel` (текущий, см. выше) — старый адрес
    Netlify больше не резолвится, если где-то остался старый ярлык на телефоне — пересохранить.
- **Следующий шаг** — не определён, ждать новых пожеланий Ирины.

### Планировщик расходов — идёт пересборка (с июля 2026)
Пересборка ведётся по структуре и логике из файла, который Ирина прислала отдельно (не
файл-выгрузка живой таблицы выше — часть вещей в новой версии будет с нуля, часть —
переиспользуется из уже работающей автоматизации в Код.js, см. выше). Работаем и пушим
в `main` этого репозитория напрямую (не в отдельных ветках) — так данные остаются свежими
из сессии в сессию. Подробности решений по пересборке — по ходу работы, актуальное держать
здесь компактно после завершения этапа, не по каждому шагу.

