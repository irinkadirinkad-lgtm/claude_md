# Проекты — стабильные факты

Один файл на все действующие проекты (не по файлу на проект) — раздел `##` на каждый.
Живой статус/TODO по каждому проекту — в `STATUS.md` его собственного репозитория, не здесь.

## Где живут скрипты в Google Диске

Все Apps Script проекты и служебные таблицы лежат в ОДНОЙ папке «Мой диск» → **COWORK**
(`1wq5zx0DwObonCu_GnXHexltG8Id5wa0M`). Там же журналы интеграций и файлы, к нам отношения не
имеющие (`*.conf`). Прямая ссылка на любой скрипт: `https://script.google.com/d/<scriptId>/edit`.

⚠️ **Три файла с почти одинаковым началом имени — не перепутать при уборке:**
«Финансовая отчётность — **API приложения**» и «— **интеграция YClients**» ЖИВЫЕ и нужны;
«— **служебный**» (сборщик этапа 1) убран 02.08.

## Индекс проектов (куда смотреть — чтобы не перечитывать всё)
- **Yclients Finance App** (репо `Yclients_integration_tables_application`) — раздел ниже. Внутри:
  - Единая таблица «Финансовая отчётность»: формулы, интеграция YClients (Блок A), приложение (Блок C).
  - Приложение «Финансовая отчётность» → **https://elnaturel-finance.web.app** (подраздел «Приложение… (Блок C)»).
  - Планировщик расходов + форма/фронтенд `planirovshik.web.app`.
  - Сотрудники финансы (старый просмотрщик `elnaturel.netlify.app` / viewer+viewer-api удалён 14.07, заменён приложением выше).
- **Интеграция Тильда ↔ YClients (магазин)** (репо `tilda-yclients-integracia-magazin`) —
  раздел ниже. Замена МойСклада: остатки/цены из YClients на сайт (Поток A, на автопилоте),
  заказы с сайта → списание в YClients (Поток B). Свой Apps Script, свои ключи.
- **Интеграция Ozon ↔ YClients (маркетплейс)** (репо `ozon-yclients-integracia`) — раздел ниже.
  Только FBS, склад «BLOOMES». Остатки YClients → Ozon (Поток A, на автопилоте с 30.07.2026);
  заказы Ozon → списание в YClients (Поток B) — ещё не построен. Свой Apps Script, свои ключи.
- **Инвестиции** (личный учёт Ирины: облигации, акции, купоны) — раздел ниже. Google-таблица
  «Инвестиции — учёт»; Ирина шлёт скрин сделки → ассистент вносит, формулы считают всё сами.

## Yclients Finance App

Репозиторий: `Yclients_integration_tables_application` (main). Автоматизация финансовых
операций салона и магазина — форма/журнал → YClients, Планировщик расходов,
импорт сотрудников, приложение-просмотрщик. Живой статус/ход работы по ВСЕМ блокам
(1/2/3) ниже — в `STATUS.md` репозитория `Yclients_integration_tables_application`, не здесь.


### Текущее состояние — готово и работает

#### Google Таблица «Финансовые операции — рабочая таблица» (= СИСТЕМНАЯ таблица)
https://docs.google.com/spreadsheets/d/1gt3dtbbx-JigKtlPA9AdicRNewMDy29M86xaFSvgbaU/edit
- Лист 1 (журнал, после чистки 08.07.2026): A Дата, B Поставщик, C ID поставщика, D Статья,
  E ID статьи, F Касса, G ID кассы, H Комментарий, I Сумма, J Отправлено в YClients.
  Журнал — очередь «в моменте», не архив: строки, ушедшие в YClients, чистятся (старше
  1 мес. — автоматически 1 числа, решение Ирины 14.07.2026; полная чистка —
  `?action=cleanupAllSent` скрипта планировщика).
- Лист «Справочники»: Тип, Название, ID
- Служебные листы (тоже здесь, НЕ в таблице Планировщик): «Webhook лог» (входящие вебхуки
  YClients — с 10.07.2026 фильтр: пишутся ТОЛЬКО `finances_operation`, доход+расход;
  record/client/staff/goods_operations_* отбрасываются на входе как шум, к доходам/расходам
  не относятся. Автоочистка раз в месяц, хранение 1 мес.), «Статус автоматики» (последний
  запуск каждого процесса: время, ✓/✗, подробности — сюда пишут ОБА скрипта).

#### Скрипт журнала «Финансовые операции» — Apps Script Web App
Деплой: https://script.google.com/macros/s/AKfycbwb4Xb2u8WNO9bHOes1MihWH8FeI5jExClurhtVQpkpSVgP-Yw2anyVuow6upo-39w/exec
Standalone-скрипт (scriptId в корневом `.clasp.json` репозитория), файлы: `Код.js`,
`index.html` (розовая форма при прямом заходе), `appsscript.json`.
- JSON API (`?data={"action":...}`): getReferenceData / addReferenceItem / addEntry /
  setupTriggers / listTriggers. Этим API пользуется вкладка «Ввод» приложения
  planirovshik.web.app.
- `addEntry` пишет строку в журнал и СРАЗУ вызывает `sendToYClients()` (мгновенная отправка);
  триггер `sendToYClients` раз в 6 часов — подстраховка-повтор для неотправленных строк.
- Старый конвейер планировщика (updatePlanner/closeMonthAndUpdate) УДАЛЁН 08.07.2026.

#### Доступы к YClients
Хранятся в Apps Script → Project Settings → Script Properties (НЕ в коде, НЕ в репозитории):
PARTNER_TOKEN, COMPANY_ID=328251, YC_LOGIN, YC_PASSWORD, USER_TOKEN.
Технический пользователь YClients создан специально для интеграции (не личный аккаунт).

#### Справочники — сверены с YClients
- Статьи платежа: 39 пунктов с реальными ID (и доходные, и расходные категории — единый
  список, как в YClients)
- Кассы: 6 пунктов, ID подтверждены
- Контрагенты: список подтверждён (~25 пунктов)
- Источник истины — `KASSY_BASE`/`STATYI_BASE` в `Код.js` (используется формой). Полный
  список с ID также лежит отдельным файлом в Drive: «Для планировщика расходов.xlsx»
  (id `1q-hghgEWWKP_2hj9Zdedm_eqzJTcD61x`) — держать под рукой при работе с доходом из
  YClients (сопоставление `expense.id`/`expense.title` из API с нашими категориями).

#### API YClients — подтверждено диагностикой
- `POST /api/v1/finance_transactions/{company_id}` — создание операции (работает).
  Поля: expense_id, amount, account_id, client_id, supplier_id, master_id, comment, date ("YYYY-MM-DD HH:MM:SS")
- `GET /api/v1/finance_transactions/{company_id}` — **405 Method Not Allowed**, читать через
  этот эндпоинт нельзя (только создание).
- Авторизация: Authorization: Bearer {partner_token}, User {user_token}
- user_token: POST /api/v1/auth {login, password}
- `GET /api/v1/staff/{company_id}` — 200 OK, список сотрудников (id, name, specialization,
  position{id,title}, fired, status).
- `GET /api/v1/storages/{company_id}` — 200 OK, список складов. У салона их 2: id 637822
  «Основной склад» (for_service — расходники на услуги), id 637823 «Магазин»
  (for_sale — розница/косметика).
- `GET /api/v1/records/{company_id}` — **200 OK** (после того как Ирина расширила права
  техническому пользователю 04.07.2026). Фильтры: `start_date`, `end_date`, `count`. Ключевые
  поля записи: `staff_id` (кто оказал услугу), `created_user_id` (кто оформил визит —
  например, id администратора Элины) — то самое разделение «мастер обслужил» vs
  «администратор записал», которое нужно было для Ввода. `services[]` (title, cost) —
  выручка услуг. `goods_transactions[]` (title, cost, master_id, storage_id, good_id) —
  товары, проданные В РАМКАХ визита. `attendance`/`visit_attendance` — статус посещения
  (1 = пришёл).
- **ВАЖНО про «новый клиент»:** `client.is_new` и `client.success_visits_count` внутри
  записи — это ТЕКУЩЕЕ состояние клиента на момент запроса API, а НЕ историческое состояние
  на момент того визита. Проверено на реальных данных (04.07.2026): у клиента с несколькими
  визитами за 4 месяца `success_visits_count` ОДИНАКОВЫЙ на всех его визитах (например,
  везде «52») — это просто «сколько визитов у него сейчас», не история. Значит: **нельзя
  брать эти поля из старой записи и считать, что они отражают состояние клиента в дату
  записи.** Единственный надёжный способ определить «это был первый визит клиента» —
  считать самим: найти дату самого раннего визита клиента по ВСЕЙ его истории (не только
  за целевой период) и сравнить с датой конкретной записи. Иначе бэкфилл старых месяцев
  задним числом будет давать неверные цифры по новым клиентам.
- `GET /api/v1/schedule/{company_id}/{staff_id}/{start_date}/{end_date}` — **200 OK** (пути
  через `/`, не `?query`). Отдаёт массив по дням: `{date, is_working, slots:[{from,to}]}` —
  это и есть настоящие «Смены», не привязанные к визитам (ровно то, что просила Ирина).
- **Розничные продажи БЕЗ визита — РЕШЕНО, полностью читается задним числом.** Не через
  `records` (там их нет — проверено за 4 месяца, 0 совпадений) и не через `storage_operations`/
  `goods_operations`/`documents` (это всё эндпоинты СОЗДАНИЯ, не чтения — 404 или 400 на
  чтение). Правильный путь нашёлся в официальной документации YClients (не в JS-версии сайта,
  которую я не могла прочитать, а в разделе API-документации, куда Ирина зашла напрямую):
  - **`GET /api/v1/transactions/{company_id}`** — НЕ `finance_transactions` (это другой,
    create-only ресурс). Фильтры: `master_id`, `start_date`, `end_date`, `client_id`,
    `account_id`, `type`, `page`, `count` и др. Возвращает ВСЕ финансовые операции сотрудника
    за период — включая продажи товара БЕЗ визита (`record_id:0, visit_id:0`,
    `expense:{id:7, title:"Продажа товаров"}`) и, что важно, **выручку по услугам тоже**
    (`expense:{id:5, title:"Оказание услуг"}`) — с привязкой к `master_id` в обоих случаях.
    Также встречаются служебные записи типа зарплатных выплат (`expense.id:3`,
    отрицательная сумма) — их отфильтровывать, брать только `expense.id` 5 и 7.
  - Проверено на реальных данных (04.07.2026): по мастеру Луизе за тестовый период отдельно
    просуммировались услуги (`id:5`) и товар (`id:7`), суммы разумные.
  - **Вывод: `transactions` — единственный нужный источник выручки** (и услуг, и товара,
    визит был или нет). Парсить `goods_transactions[]` внутри `records` для товара больше
    не требуется — всё покрывает `transactions`. `records` остаётся нужен только для
    подсчёта визитов/клиентов/нового клиента и разделения мастер (`staff_id`) vs
    администратор (`created_user_id`).
  - Вебхуки (`hooks_settings`) для этой задачи больше НЕ нужны — реальное время не
    требуется, `transactions` и так даёт полную историю назад (можно бэкфилить март-май).

#### Планировщик расходов (таблица Планировщик) — итоговая архитектура с 08.07.2026
**Таблица:** «Планировщик расходов» id `1CYDwiM8bMSRAwLdXK9b0ZX3ulo4J85myW0gHKSsmcLI`,
ровно 3 листа: «Планировщик расходов» (текущий месяц), «История» (архив месяцев, подписи
в строке 2 с колонки C), «Справочники». Никаких служебных листов (они в системной таблице).
СТАРАЯ таблица `12zlcXPzG...` — статичный архив в Drive, никто не пишет и не читает.

**Принцип: YClients — единый хаб.** Ручной ввод (форма → журнал) мгновенно уходит В
YClients; Планировщик наполняется ТОЛЬКО ИЗ YClients. Каждый запуск синхронизации — ПОЛНЫЙ
пересчёт текущего месяца с нуля (перезапись строк-категорий суммами операций месяца):
дублей нет по построению, правки/удаления операций в YClients подхватываются сами.

**Скрипт:** `planner-new/` в репозитории (scriptId `1xoTRzb3YP60UoFJDNxspGzoDkJpfePZ9eQ3hqYPKo-SZBEaLSkp9tu4Z`,
деплой `AKfycbzS77EUbsUFhDW_RiRD0vzkPWnODRrpcF_KPjmi44TQ2nijMHLgqxaD0j1klKx2wW_hdw`,
`ANYONE_ANONYMOUS`). Ключевое:
- `doPost` — приёмник вебхука YClients (Marketplace-приложение «Непубличное», Application
  ID 46590). YClients шлёт вебхуки на всё подряд; в лог пишем и на пересчёт реагируем ТОЛЬКО
  `resource==='finances_operation'` (фильтр 10.07.2026, см. лист «Webhook лог» выше) →
  мгновенный пересчёт. Триггер `cleanupWebhookLog_` самоустанавливается из
  `pullAllYClientsTransactions_` по флажку `WEBHOOK_CLEANUP_TRIGGER_SET` в Script Properties
  (без ручного setupTriggers).
- `pullAllYClientsTransactions_` — полный пересчёт месяца (вебхук / триггер 6 ч / вручную),
  LockService + тихий пропуск при наплыве. Знак расходов YClients — минус (инвертируется
  для строк 28-59). Операции без категории (`expense` пустой) — переводы между кассами,
  пропускаются. Карта категорий: `STATYI_BASE_` (копия из журнального скрипта; id 294038
  «НДС 22% за эквайринг» → строка «Комиссия за эквайринг»).
- Месяц ВЕЗДЕ считается одним кодом из YClients: `computeMonthFromYClients_` (расчёт) +
  `writeMonthToHistory_` (запись столбца «Истории»). Ими пользуются закрытие, бэкфилл и до-счёт.
- `backfillMonthFromYClients_` (`?action=backfillYC&year&month[&dry=1]`) — загрузка ЕЩЁ НЕ
  закрытого месяца в «Историю»; dry=1 — посчитать без записи. Уже закрытый месяц — не сюда,
  а `refreshPrevMonth` (иначе появился бы второй столбец того же месяца).
- `closeMonthAndUpdateNew_` (1/2/3 числа, 01:00) — архив месяца в «Историю» + сброс +
  пересчёт нового. Архив берётся ИЗ YCLIENTS (не снимок листа — он может быть затёрт, см.
  инцидент 01.08.2026 в STATUS.md). Три проверки до записи: несовпавшие статьи → стоп+письмо;
  пусто в YClients → стоп+письмо; расхождение с листом > 1 ₽ → пишем по YClients + письмо.
- **«Окно ЗП» — `refreshPrevMonthWindow_` (1-5 числа, 05:00, ставится сам, флажок
  `SALARY_WINDOW_TRIGGER_SET`).** Зарплата вносится задним числом (30/31) уже после закрытия
  месяца, поэтому 1-5 числа прошлый месяц пересчитывается и его столбец ПЕРЕЗАПИСЫВАЕТСЯ
  (`refreshArchivedMonth_`, идемпотентно). Вручную — `?action=refreshPrevMonth[&year&month]`.
- `cleanupOldJournalRows_` (1 числа, 03:00, хранение 1 мес.);
  `cleanupWebhookLog_` (1 числа, 04:00, хранение 1 мес.).
- Служебные doGet-действия — только с `?key=ADMIN_KEY` (Script Properties). Полный список
  выдаёт сам doGet без action.
- **Маркетплейс-выручка (Ozon/Я.Маркет) — особый учёт** (важно при сверке!): Ирина проводит
  её ПО ФАКТУ поступления денег (за вычетом комиссии, приходит позже), а НЕ в момент продажи/
  списания товара со склада. YClients на списании может показать товар «как проданный» в
  Аналитике, хотя денег ещё нет — не считать это расхождением планировщика. Планировщик прав:
  берёт реально проведённые деньги. (Из разбора расхождения января 08.07.2026 — 1 360 ₽
  маркетплейс-товара; см. STATUS.md репозитория.)
- Строки листа «Планировщик расходов»: 13 Выручка (без строки 22 «Пополнение кассы» — не
  выручка), доход 15-24, расход постоянные 28-39, переменные 40-59, итоги 61-72.
  В «Истории» расходы со сдвигом −2 (26-57), итоги 59-69.

**Просмотрщик для приложения:** `planner-viewer/` в репозитории (scriptId
`1pSvtsCBCNllWSxeb7n6qemlGOVQzvpf9NsYM-AJ78a9D6EGDL9p5P1AB`, деплой
`AKfycbzMwEvJUtreH1-kVX1J4qvDODo5extq-x5EWe3MrRFJNsVQ1oZSbjs3CvJDmHP1Eak5rQ`) — read-only
JSON (monthOptions/dashboard/reserveByMonth) для вкладки «Планировщик» приложения.

**Приложение Ирины:** https://planirovshik.web.app (Firebase Hosting) — вкладка «Ввод»
(JSON API журнала) + вкладка «Планировщик» (planner-viewer). Исходник фронтенда теперь В
РЕПОЗИТОРИИ: подпапка `planirovshik-app/` (index.html + иконки + manifest + firebase.json +
.firebaserc). Раньше исходника не было (только живой деплой) — заведён при редизайне 08.07.2026.
- **Firebase:** проект (Project ID) — **`finance-elnaturel`**, НЕ «planirovshik». `planirovshik`
  — это hosting-САЙТ (target) внутри проекта (второй сайт там же — `finance-elnaturel.web.app`).
  Деплой из `planirovshik-app/`: `firebase deploy --only hosting:planirovshik --project finance-elnaturel`.
- **firebase login в контейнере — капризный:** только через СТАРУЮ версию CLI
  (`npm i -g firebase-tools@11.30.0`) и pty-обёртку (в новых версиях ссылка auth.firebase.tools
  даёт «Unable to verify client» — сервер придирчив к IP контейнера; прошло с 3-й свежей ссылки,
  открывать СРАЗУ). Ирина открывает ссылку с телефона, присылает код. REST-деплой через
  clasp-токен НЕ работает (токен от чужого GCP-проекта Apps Script, Firebase Hosting API
  выключен, quota-project сменить нет прав). Детали — в STATUS.md репозитория.
- **Стиль (утверждён Ириной 08.07.2026):** шрифт **Arsenal** (вшит data-URI), liquid glass
  (прозрачные плашки), логотип-тюльпан справа сверху, вкладки-таблетка; заголовки КАПСЛОКОМ,
  мелкие подписи строчными; светлая+тёмная темы идентичны. Кнопка «Только Планировщик» УБРАНА.
  Есть **десктоп-раскладка** (`@media min-width:860px` — во всю ширину, форма 2 колонки,
  планировщик-дашборд; телефонный вид не тронут). Кнопка **обновления** (↻) в Планировщике.
- **Кэш:** `firebase.json` отдаёт index.html с `Cache-Control: no-cache, no-store` —
  приложение всегда грузит свежую версию (раньше не обновлялось после правок).
- **Выпадающий список формы** (поставщик/статья/касса) выносится JS-ом в конец страницы
  (`position: absolute`, координаты страницы) — иначе `backdrop-filter` карточки ломал
  z-index (список уходил под поля). Пункты — ЗАГЛАВНЫМИ. Комментарий — свободный регистр
  (`#f-comment { text-transform: none }`), остальной ввод — строчными.

#### Доступ к Apps Script этого проекта (clasp)
Ирина согласовала: Claude может сам авторизоваться через `clasp` и напрямую пушить код
в Apps Script проект «Финансовые операции», а также читать логи выполнения — без
промежуточной пересылки файлов/скриншотов туда-обратно. Общий механизм (токен не переживает
сессию, нужен повторный логин) — см. CLAUDE.md, раздел про доступ к внешним инструментам.

**Более быстрый способ авторизации clasp (обнаружено 04.07.2026, из ветки main):** вместо
интерактивного `clasp login` каждый раз, можно прочитать уже готовый `~/.clasprc.json` из
Google Drive — файл «CLAUD системные/clasp-credentials.json» (id
`121GGUUB7g8TFzGfkoCQwnefA0xdW_Swv`), записать его содержимое в `~/.clasprc.json` на диске,
и `clasp` сразу заработает без ссылки-авторизации через Ирину. Проверить, что токен внутри
ещё не протух (refresh_token обычно долгоживущий) — если протух, тогда уже обычный
интерактивный логин.

**Production deployment ID** (веб-приложение, `AKfycbwb4Xb2u8WNO9bHOes1MihWH8FeI5jExClurhtVQpkpSVgP-Yw2anyVuow6upo-39w`)
— после `clasp push` живой URL обновляется только через `clasp deploy -i <этот ID>`
(деплой с `ANYONE_ANONYMOUS` не следует за @HEAD). То же правило — у всех остальных
скриптов проекта (planner-new, planner-viewer, employees-integration, viewer-api).

GitHub Actions-автодеплой удалён 08.07.2026 (дублировал ручной clasp-процесс и, вероятно,
провоцировал слёты авторизации: каждый git-пуш дёргал живой скрипт). Пуш в `main` — теперь
просто git; деплой — только осознанно через clasp.

**Известная особенность:** после правок кода скрипта Google может молча отзывать
авторизацию веб-приложения (Access Denied). Фикс: Ирина открывает script.google.com →
проект → выбрать функцию (годится `doGet`; функции с `_` в списке не видны) → ▶️ Выполнить →
подтвердить права. Повторяется при активной разработке — это не поломка.

### Единая таблица «Финансовая отчётность» (мастер-проект)

Три блока: (A) формулы таблицы + интеграция YClients → таблица — **ГОТОВО (11.07.2026)**;
(B) онлайн-каналы (сайт/маркетплейсы, отдельная таблица) — дальше; (C) приложение — дальше.
Живой ход/грабли — в `STATUS.md` репозитория `Yclients_integration_tables_application`.

**Таблица:** «Финансовая отчётность», Google Sheets id `1Hpe21syTpJVl3E6LVHdznRhjRPXvmeGpwJTF3lhpGMM`
(конвертирована из xlsx `13coDHGK...` — он остался архивом). Пояс Europe/Moscow. 12 листов;
источники: «Настройки» (параметры, датированные истории ставок/себестоимости, справочник
сотрудников) и «Ввод» (одна строка = сотрудник за месяц), остальные 10 считаются из них.
Серые ячейки с примечанием = авто (не вводить руками).

**Датированные ставки** — «Настройки» → «ИСТОРИЯ ОБЩИХ СТАВОК» (A29:J36): строка действует
с указанной даты до следующей (налог, эквайринг, % мастера, ставки, целевая выручка).
Формулы разрешают строку по месяцу отчёта через `LOOKUP` — закрытые месяцы не пересчитываются.
⚠️ Колонка I «Целевая выручка» = минимум месяца; в ПОСЛЕДНЕЙ строке там не число, а ссылка
`='Расходы План'!$B$30` (точка безубыточности), поэтому текущий минимум не устаревает; период
замораживается автоматически (`freezeMinPeriod_`). ⚠️ Столбец I ниже по листу (I65:I200) занят
справочником сотрудников — колонку не удалять и не сдвигать. Детали — в `STATUS.md` репозитория
`Yclients_integration_tables_application`.

**«Ввод» — раскладка v3 (данные с строки 5, формулы до 104, ниже скрыто):**
A Месяц (дата 1-го) | B Сотрудник (ключ) | C Направление | D Тип оплаты | E Смены | F Клиенты |
G Новые | H УслугиНал | I УслугиБезнал | J УслугиИТОГО* | K ТоварНал | L ТоварБезнал |
M ТоварИТОГО* | N Кол-во продаж шт | O Себестоимость товара | P..AF расчёт* (средний чек,
ставки, ЗП, эквайринг, налог, чистая прибыль, доли) | Y Себестоимость услуг (материалы, значением).
Входные: E,F,G,H,I,K,L,N,O,Y (из YClients интеграцией). * = формулы.

**Ключевые правила модели (решения Ирины 10.07.2026):**
- Услуги компании = только мастера (Маникюр/Парикмахерский зал/Арт - Директор). У админов
  клиенты/новые/«выручка в смены» — записанные ими, ТОЛЬКО их аналитика, в общие суммы не входят.
  Товар — все строки (мастера+админы+«Без сотрудника — продажи»).
- Эквайринг — только на безнал; налог — со всей выручки.
- Чистая прибыль (лист «Прибыль») исключает из «Расходов (факт)» статьи-дубли ДР Факт:
  Налоги и сборы, Зарплата персонала, Закупка материалов, Закупка товаров, Комиссия за
  эквайринг (строки 42/43/46/47/60). Кредит — целиком. Закупки ≠ себестоимость (склад vs факт).
- «Пополнение счета/кассы» — не доход (в «Итог: Доходы» ДР Факт не суммируются).
- Себест. услуги ВИДНА в приложении (вкладка «Услуги», оба рейтинга — за месяц и за всё время):
  столбец «себест.» = материал за штуку, по длине (окрашивание короткие/средние/длинные отдельно).
  Источник — столбец J «Себест. за шт» листа «Услуги детально» (пишет интеграция, застыло помесячно);
  app-api отдаёт `servicesRaw`/`allTimeRaw` (по длине). Круги долей/помесячная — свёрнуто, как раньше.
- Себестоимость ТОВАРА — из карточек YClients (кол-во × закупочная), интеграция пишет в O.
  Себестоимость УСЛУГ — ПО МАТЕРИАЛАМ (Σ материал услуги × кол-во по факту визитов, мультиуслуги —
  каждая своя). Интеграция пишет в **«Ввод»!Y значением** (застыло → закрытый месяц не пересчитывается
  при смене цен), «Прибыль» стр.44/45 (SUMIFS Y по направлению) → 46 → чистая прибыль. Материалы —
  из живых файлов модели Ирины (правит цену → пересчёт сам). Только материалы (ФОТ/налог/эквайринг/
  постоянные вычитаются отдельно). Детали, ID файлов, решения по услугам — в STATUS.md репозитория
  `Yclients_integration_tables_application` («Себестоимость услуг по материалам»). Старый расчёт
  «F × норматив» оставлен как fallback.
- Эквайринг — РЕАЛЬНАЯ комиссия банка (карта 3,05%/QR 0,7%, статья 11 YClients), не плоские 3%.
  Кредит и аренду вычитаем в чистую прибыль ЦЕЛИКОМ (реальные обязательства — решение Ирины 11.07).
- Сотрудники «только в данных» (уволенные, напр. Анастасия): в справочник НЕ заносить;
  строка в «Ввод» + «% от услуг» числом. Товар под карточкой Ирины → «Без сотрудника — продажи».
- Менять ставки/себестоимость = ДОБАВИТЬ строку с датой в историю (прошлые месяцы не трогаются).
- Авто-закрытие месяца — через авто-прогон интеграции (autoRun, 4 раза в сутки), а не формулами.
- «Расходы План» — усреднённый ориентир, «в день» = /30 фикс, к месяцам не привязывать.

**Служебный скрипт этапа 1 `finance-report-builder` УБРАН (02.08.2026):** этап закрыт, живых
потребителей не было. Деплой снят — открытый анонимный адрес мёртв, папка из репозитория
удалена. Сам файл в Drive («Финансовая отчётность — служебный») удаляет Ирина: правами clasp
чужой файл не тронуть.

**Интеграция блока A — ГОТОВА (11.07.2026).** Отдельный изолированный Apps Script `finance-integration/`
(standalone, тянет из YClients, таблицы между собой НЕ связаны; расписание, без вебхука).
- **scriptId** `1ST_zHvsyOfUj_tvnsQEiirNxepqs1rlRed6djejNioPfC7j8i6CYK4mq`; **деплой** (web app,
  ANYONE_ANONYMOUS; служебные действия — ТОЛЬКО владельцу по `Session.getActiveUser()`,
  пароля в коде нет с 26.07.2026; исключение — `appDay`/`appRange` под `APP_KEY`, только чтение)
  `AKfycbwKyxSZh6BwBLB-twobNr9jhK_Z8_b5onpC0A58PfpF4rlUMGrDJuYMwfyMtYtzDgDseg`.
  Script Properties: PARTNER_TOKEN, COMPANY_ID=328251, YC_LOGIN, YC_PASSWORD, USER_TOKEN(сам).
- **Действия (doGet, гейт key):** `runMonth&month=YYYY-MM` (записать месяц); `previewMonth/previewDay&date=/
  previewRange&start=&end=` (расчёт без записи — для приложения); `previewCategories`; `setupServices`
  (каркас блоков услуг-категорий), `setupDrFakt` (формула B), `setupAutoTrigger` (триггер autoRun);
  `servicesDetail&month=` (запись детализации услуг) / `previewServicesDetail&month=` (read-only);
  `info/checkAuth`. Ядро прогона — `runMonthDate_` (пишет «Ввод», категории, «ДР Факт» И «Услуги
  детально»). Авто: триггер `autoRun` **4 раза в сутки — 6:15, 12:15, 18:15, 21:30**
  (`AUTO_SCHEDULE_`; до 04.08.2026 был один утренний, из-за чего внесённое вечером появлялось
  только назавтра — разбор в `STATUS.md` репозитория `Yclients_integration_tables_application`).
  ⚠️ **21:30 — не круглое число наугад:** рабочий день заканчивается в 21:00, к 21:30 операции
  дня внесены, и Ирина видит итоги дня сразу, не дожидаясь ночи (её просьба 04.08). Минуты
  разведены с чужими расписаниями: Тильда сверяется в :00 каждого часа, Ozon — в :35 в часы
  1/4/7/10/13/16/19/22, наши часы с ними не совпадают. Считает текущий
  месяц, а **1–5 числа** ещё и прошлый — закрытие месяца задним числом. Идемпотентно
  (перезапись, не наслоение), прогон ~60 с, защищён `LockService` от наложения прогонов.
  ⚠️ Служебные действия идут через ДЕПЛОЙ, а он не следует за `@HEAD`: после `clasp push`
  обязательно `clasp deploy -i <тот же id>`, иначе `?action=…` выполнит старый код (наступил
  на это 04.08 — `setupAutoTrigger` поставил один триггер вместо четырёх).
- **«Услуги детально» (лист-источник, СКРЫТ; решение Ирины 12.07):** детализация до КОНКРЕТНЫХ услуг
  (в таблице раньше только категории). Одна строка = месяц × мастер × услуга (кол-во + выручка),
  из YClients `records.services` (staff_id→имя, категория из `service_categories`). Фиксируем в
  таблице (не вживую) — ради быстрого доступа и статистики по месяцам. Питает раздел «Услуги» и
  «Топ услуг» мастера в приложении. Модификаторы чека («+20% за длину» и т.п.) — отдельными строками.
  **С 17.07 — принцип «backbone скрыт + зеркало»:** app-api и QUERY читают этот скрытый лист без
  изменений, а Ирина видит стилизованную детализацию ВНИЗУ видимого листа «Услуги» (зеркало,
  раскраска по месяцам). Аналогично клиенты (ниже). Детали — STATUS.md репозитория Yclients.
- **Модель атрибуции (выверена на живых данных):** мастер — серверным фильтром `master_id`
  (в операции `master` пустой); услуги=статья 5, товар=статья 7; нал/безнал по кассе (`is_cash`).
  Админ — аналитика по визитам `created_user_id` (в общие суммы не входит). Двойная роль Ирины —
  одна карточка YClients id 969478 = обе строки, товар считаем 1 раз. Кол-во/себестоимость товара —
  из `storages/transactions` (type 1) + карточка `goods/{company}/{good_id}`.`unit_actual_cost`.
- **«ДР Факт»** — статьи из тех же транзакций (копия `STATYI_BASE_` Планировщика), `B`=формула по
  выбранному месяцу, E:P=история, Q=Итого 2026. Строки-итоги по месяцам: «Итого доходы» (стр. 30,
  без пополнений счёта/кассы) и «Итого расходы» (стр. 65). **Категории** — товар по брендам («Продажи»),
  услуги по категориям на листе **«Услуги»** (строки 100+, живые формулы из «Услуги детально», ₽+шт +
  «Итого 2026»; БЕЗ Анастасии — как в приложении). **Эквайринг** — реальная комиссия (статья 11).
- Детали (структура блоков, строки, грабли API) — STATUS.md репозитория `Yclients_integration_tables_application`.

#### Приложение «Финансовая отчётность» (Блок C) — ПОСТРОЕНО (12.07.2026)
Мобильное приложение под таблицу «Финансовая отчётность» (Блок A), в стиле Планировщика
(liquid glass/Arsenal, светлая+тёмная темы). ОТДЕЛЬНОЕ, не трогает Планировщик и старый viewer.
- **Фронтенд:** `finance-report-app/` в репозитории. Firebase Hosting, проект `finance-elnaturel`,
  сайт `elnaturel-finance` → **https://elnaturel-finance.web.app**. Иконка — фирменный цветок
  (файлы `flower-*.png`). Самодостаточный `index.html` (шрифт/логотипы data-URI).
- **Пароль на входе (14.07):** экран-гейт `.gate` + `<body class="locked">`, разблокировка снимает класс
  (оверлей поверх, appWrap не прячется → графики не ломаются). SHA-256 на клиенте (хеш `89638b…`, тот же
  пароль, что был на старом viewer), флаг `gatePassed` в localStorage. Осознанно слабая защита — от
  случайной утечки ссылки, не от взлома (решение Ирины). Сброс входа = очистить данные сайта.
- **Бэкенд:** `finance-report-app-api/` — отдельный Apps Script, ТОЛЬКО ЧТЕНИЕ (изолирован от
  `finance-integration/`, где есть запись). scriptId `1ilQnPzt9pxWXRk1F4M6vMaAJ7e5H-CeTcHXeMlVBvha-xiK5heev7q2Q`,
  деплой `AKfycbxwpqYCDc1y0G_4CqACUHih6fNuL0Px2BM96WfaSug-3cZqdOje3GRt8tnAOezyRBcbjg`
  (ANYONE_ANONYMOUS, executeAs владелец, без ключа — как viewer-api). Действия: `monthOptions`,
  `overview`, `history`, `plan`, `sales`, `salesHistory`, `directions`, `drfakt`, `masters`, `services`,
  `fot`, `clients` — читают готовые значения листов, ничего не считают сами.
- **Вкладка «Аналитика дня» (14.07):** ЖИВЬЁМ из YClients ПО КНОПКЕ (не app-api, а отдельный экшен
  `appDay?date=`/`appRange?start=&end=` у скрипта-`finance-integration`, гейт Script Property `APP_KEY`=`k`).
  Выбор день/период, ленивая загрузка + спиннер. Порядок (15.07): **загруженность СВЕРХУ круговым графиком** (крупный;
  круг = **100% ёмкости салона**: занятая часть = загрузка салона, делится между мастерами по их загрузке, остальное —
  «свободно» серым; на выноске ИМЯ+% мастера, «свободно» — только в легенде; + «загрузка салона») → Прибыль дня
  (доход − эквайринг(расч.) − налог 7% − себест.услуг − себест.товара − ФОТ = чистая; порядок плашек: продажи товара
  ПЕРЕД себест.товара, ФОТ ниже) → Клиенты дня → **Сотрудники** (ТОЛЬКО фактически работавшие): МАСТЕРА — смены/
  клиенты(+новые)/услуги/товар/заработал/загрузка + **список услуг за день** (`servicesList`); АДМИНЫ — смены/
  клиенты(+новые)/**выручка** (=доход дня)/товар/заработал/**загрузка САЛОНА** → **Направления → услуги** (направление,
  под ним конкретные услуги: сумма/штуки; `collectCategories_.items`) + круг долей (от 2+) → Касса (БЕЗ эквайринга) +
  **приход/расход по счетам с разбивкой ПО СТАТЬЯМ** (за что; `byAccountDetail` из `expense.title`). Загруженность —
  `analytics/overall` `fullness_stats`.
  ⚠️ Загруженность: формула YClients не документирована (наблюдаем). collectRange_ 2-лет скан «новых» НЕ трогали —
  кормит KPI-ЗП админов. **Фильтр «работал»:** админ = `shifts>0` (клиенты-«записал» по `created_user_id` НЕ считаются
  работой → «Ирина» больше не всплывает); мастер = `shifts>0` ИЛИ `servicesRevenue/goodsRevenue>0`. Круги — общий
  `calloutPieSvg({geom,labelFont,inside,labelFmt})`.
  Круговые графики — общий `calloutPieSvg` (выноски-ответвления).
- **Вкладка «Клиенты» (14.07):** ПОМЕСЯЧНО из СКРЫТОГО листа «Клиенты» (интеграция считает `collectClientStats_`
  по записям за всю историю с 2019 → пишет столбец месяца; app-api `clients` читает). Классификация — по псевдо-
  карточке из записей (визиты/срок/траты/ритм; `clients/search` закрыт правами «управление компанией»). **6 КАТЕГОРИЙ**
  (пороги-константы `CLI_*`): Новые(1) · Присматриваются(2-3) · Лояльные(4+, не постоянный) · Постоянные(>6 виз. И
  >6 мес) · **Премиум**(>1г И визиты≥25 И траты≥150к, ранг по тратам) · Потерянные. Старшинство: потерян→премиум→
  постоянный→лояльный→присматриваются→новые. Потерян: <4 виз. — >6 мес; 4+ — >max(90д, 2×своего ритма).
  ⚠️ **Давно ушедшие** (>1 года как потерян, класс `gone`) — ВНЕ базы/статистики (иначе искажают; вернуть нереально).
  База = живая (напр. июль: 45 клиентов, 26 потерянных; без gone-фильтра было 116/97). Плашка **«активных клиентов»**
  (всё, кроме потерянных; июль 19) вверху категорий. Метрики месяца: всего/новые/«вернувшиеся» (пришли снова, не
  первый визит). **Круговой график** долей 6 категорий (крупный, НАД плашками категорий) с **выносками** (ответвления
  цветом сектора + % И внутри секторов, де-коллизия; `calloutPieSvg({inside,labelFmt})`, цвета `CLI_CAT_META`) + легенда.
  Блок **«Как часто ходят»** — средний интервал визитов по АКТИВНЫМ группам (`freqNewbie/Loyal/Regular/Luxe`; Потерянные
  ИСКЛЮЧЕНЫ — не ходят сейчас, их прошлый ритм путал). Метод: на клиента `(последний−первый)/(визиты−1)`, усреднить по
  группе (июль: Премиум ~52дн … Присматр ~356дн) + **стрелка тренда** vs 3 мес назад (↑ зелёная «чаще»/↓ красная «реже»,
  только для групп ≥3 клиентов). Личная частота КАЖДОГО клиента в списках («раз в N») + **личный тренд** (стрелка ↑чаще/
  ↓реже: недавний ритм vs более ранний) — `avgGap`/`recentGap`/`priorGap` в `КлиентыСписок` кол.F/G/H → app-api
  `avgGapDays`/`recentGapDays`/`priorGapDays` (трекинг встроен в существующий лист, ТРЕТИЙ не заводили). Отдельный
  график **«Новые клиенты в месяце»** (`S.newClients`, по факту записи). **Удержание:**
  плашка «вернулись после потери» = «N чел / %»
  `reactivatedPct` (разрыв >6 мес → `maxGap`; СРЕДИ АКТИВНЫХ, знаменатель = активная база без потерянных, июль 12/19=63%)
  + доля повторных + отток (СНИМОК «сколько базы сейчас в статусе потерян», не поток). Топы: плашки топ-5 без подписей +
  два **выпадающих топ-10 с именами** (по тратам/визитам). Динамика — 6 групп на графике, **количество подписано НАД
  точками** (`opts.showValues`, де-коллизия по столбцу; таблицу количеств убрали — Ирине нужны цифры на самом графике).
  Служебное: `showClientSheets`/`hideClientSheets` — показать/скрыть листы «Клиенты»/«КлиентыСписок». ⚠️ Грабля вёрстки: `.tiles .tile` форсит column → `.tile.wide`
  (цифра справа) нужен явный `flex-direction:row`.
- **Объединение листов клиентов (17.07):** видимый лист **«Клиенты»** = сетка показателей (наш стиль,
  блоки полосами; app-api читает ПО ПОЗИЦИИ — строки не двигаем; устар. категории `regularRare`/`premium`
  скрыты, `luxe`→«Премиум») + ВНИЗУ (стр. 47) зеркало списка имён. Backbone `КлиентыСписок` СКРЫТ (его
  читает `getClientsList_`). Стилизует/зеркалит `styleClientsSheet_` из `runMonthDate_` (помесячно сам).
- **Список клиентов с именами (15.07):** отдельный СКРЫТЫЙ лист `КлиентыСписок` (интеграция `writeActiveClientsList_`,
  снимок «сейчас», обновляется на пересчёте месяца) → app-api `clientsList` (общий кэш `loadClientsListOnce` — один
  запрос на вкладку, делят группы и топ-выпадашки). В приложении — «Клиенты по категориям»: раскрывающиеся группы с
  розовой стрелочкой, **включая «Потерянных»** (кого потеряли). Внутри группы — по **последнему визиту** (недавние выше),
  все группы. Имя+буква фамилии, визиты/траты/посл.визит. Имена ТОЛЬКО тут. Служебный экшен `activeClients&month=` — та же
  выгрузка для файла Ирине. **Актуальные деплойменты (17.07): интеграция @83, app-api @51, Firebase live.** ⚠️ YClients отдаёт фамилию инициалом,
  телефон маскированно (уровень прав интеграции).
- **Меню:** Финансы / Продажи / Направления (тема — кнопкой в конце меню, попап вниз). Под-вкладки
  отдельными стеклянными таблеточками по центру. Финансы: ДР Факт(по умолч.)/Прибыль/Расходы План.
  Продажи: Общие/Магазин/Интернет-магазин/МП. «Общие» = все каналы вместе (магазин+ИМ+МП,
  строки «все каналы» = Продажи!35-36 таблицы); «Магазин» = только розница.
  Раскладку брендов в листе «Продажи» см. ниже (в блоке про интернет-магазин).
- **Онлайн-приход** берётся из реальных статей дохода «ДР Факт» (Интернет-магазин=строка 26,
  Ozon=29). Яндекс Маркет НЕ ведём.
- **Каналы разделены полностью (Блок B, 26.07.2026): магазин / интернет-магазин / маркетплейсы.**
  Признак канала — комментарий складского ДОКУМЕНТА («интернет-магазин» → сайт,
  «озон»/«ozon»/«маркетплейс» → МП, иначе магазин), читается у каждой продажи. ⚠️ Нулевая цена
  канал НЕ определяет: у продажи на Ozon она тоже бывает нулевой. ⚠️ Цена в складской операции —
  магазинная, не цена площадки, поэтому выручка онлайн-каналов берётся только из статей дохода.
  Старую операцию можно пометить задним числом действием `markChannel&doc=&text=`.
- **Интернет-магазин (детали).** Деньги канала — из проводки
  Ирины статьёй **«Интернет-магазин» (id 70466)**, сумма от клиента до налога и эквайринга,
  нал/безнал по кассе. Товар канала (штуки, бренды, себестоимость) — из складской операции сайта.
  Чистая прибыль ИМ = выручка − себестоимость − эквайринг(безнал) − налог. Разметка «Продаж»:
  30–35 ИМ, 36–40 МП, 41–43 итоги. Бренды (31.07.2026): числовой backbone 45–121 СКРЫТ —
  магазин 47–63 ₽ / 66–82 шт, ИМ 85–101 шт, МП 104–120 шт, его пишет интеграция и читает
  app-api по абсолютным строкам; видимое зеркало с 122 — три одинаковые таблицы, у магазина
  «выручка ₽ / кол-во шт» через слэш. Рублей по брендам у ИМ и МП в данных нет.
  Маркетплейсы устроены так же, но без эквайринга (площадка удерживает комиссию сама).
  ⚠️ Ozon платит с задержкой — товар и деньги попадают в разные месяцы; за год сходится.
  Подробности и проверки — в `STATUS.md` репозитория `Yclients_integration_tables_application`.
- **Доступ к данным приложения (26.07.2026):** и `app-api`, и `appDay`/`appRange` интеграции
  требуют параметр `k` = ПАРОЛЬ приложения; сервер сверяет его SHA-256 с эталоном
  (Script Property `APP_PASS_HASH`, иначе константа в коде). Хеш есть в исходниках страницы,
  но по хешу авторизоваться нельзя — «посмотреть исходник» доступа не даёт. Смена пароля —
  в трёх местах сразу: хеш на фронтенде + `APP_PASS_HASH` в обоих скриптах.
- **Деплой Hosting в сессии:** firebase CLI не нужен — через Hosting REST API с clasp-токеном
  (scope cloud-platform + заголовок `x-goog-user-project: finance-elnaturel`). Готовый скрипт
  пересоздаётся в scratchpad; шаги: обновить access_token из ~/.clasprc.json (refresh_token) →
  version → populateFiles (gzip+sha256) → upload → finalize → release. API-деплой — `clasp deploy -i`.
- **Правила визуала (соблюдать во всех блоках, ПРЕДУСМАТРИВАТЬ СРАЗУ, не ждать замечаний Ирины):**
  числа в рядах плашек ВСЕГДА выровнены по низу — **значение = последний элемент плашки, подпись СТРОГО перед
  значением** (не после), иначе разная высота подписей сдвигает цифры (`tile-label:first-child{flex:1 1 auto}` +
  подпись→значение). Круги-доли всегда = 100% (пустое/занятое честно, не нормировать к «всё занято»). Длинные
  подписи-выноски, что не влезают, — в легенду, а не обрезать. Пустых мест/пляшущих чисел не оставлять.
  Прокручиваемые таблицы/графики открываются на актуальном месяце справа; закреплённый первый
  столбец — с инсетом и скруглёнными углами; текст должен влезать (сокращать, не обрезать);
  меню НЕ липнет при скролле. Типы графиков: столбики (динамика), круги-доли (за месяц), линии
  (категории/направления по месяцам), горизонтальный цветной рейтинг (топ брендов).
- **Вкладка «Сотрудники» (12.07):** под-вкладки динамически — «Все» (сравнение мастеров +
  круг-доля выручки, отдельно таблица админов) + личная карточка на каждого (плашки, разбор ЗП,
  издержки, детали услуг/товара, доля в группе, динамика). Модель: услуги/клиенты админов — их
  личная аналитика (не в оборот компании); в компанию админ входит только ЗП/налогом. Уволенные —
  бейдж «архив». API — action `masters&month=` (одно чтение `Ввод!A5:AF104`, помесячная история).
- **Вкладка «Услуги» (12.07):** рейтинг конкретных услуг за месяц (число оказаний + выручка), под
  каждой — «кто делает» (по мастерам); в карточке мастера — «Топ услуг». API — action `services&month=`
  (читает лист «Услуги детально»). Источник данных — интеграция (см. выше). Листается по месяцам.
- Цифры СВЕРЕНЫ: аудит 2026 (янв–июл) пройден — внутренне и с живым YClients, всё сходится
  (13.07). Старый viewer `viewer/`+`viewer-api/` удалён 14.07 (осталось Ирине снести сайт в Netlify).
- Живой ход/правки — STATUS.md репозитория `Yclients_integration_tables_application`, раздел «C».

### Правила по сотрудникам (живые, применяются нынешней интеграцией)

**Двойная роль** (Ирина: Арт-Директор + Администратор) — ДВЕ отдельные строки в «Ввод»,
не пытаться разделить человека автоматически по смыслу:
- как мастер — визиты/выручка/товар по `staff_id` (кто оказал услугу);
- как администратор — клиенты/новые/выручка услуг по `created_user_id` (кого записала,
  независимо от того, кто обслуживал); смены те же, что в строке мастера;
- правило универсальное (`staff_id` vs `created_user_id`), не только для Ирины;
- товар считать по каждому уникальному id сотрудника РОВНО ОДИН раз за месяц (не по роли),
  иначе у двухролевого сотрудника товар задвоится.

**Продажи без сотрудника:** продажи Ирины (в любой роли) идут в служебную строку
«Без сотрудника — продажи», а не в её личную — чтобы не искажать её показатели
(% с продаж она не получает).

### Ушедшее на покой (не воскрешать, но и не искать заново)

- **Таблица «Сотрудники финансы»** id `1A1efYis0odAtZHh-hu10shBNf8xRIsnL` (листы Dashboard,
  Импорт, Ввод, Свод…) — предшественница нынешней книги, проект закрыт 08.07.2026. Цела,
  можно смотреть историю. Её кнопку «Импорт» обслуживает скрипт `employees-integration`
  (`1LrQhOvbjIRpfjf8m20kBixp1XJLDT85EL3nNXjY5rO-enzjaDwmNyaN8`): срабатывает только по галочке
  в той таблице, сам не запускается, никому не мешает. Ту же работу нынешняя интеграция делает
  автоматически. ⚠️ Файл в репозитории новее живого скрипта на одну правку — заливать незачем.
- **Просмотрщик `elnaturel.netlify.app`** (Блок 3) — заменён приложением «Финансовая
  отчётность», исходники удалены 14.07, сайт Ирина снесла (проверено 31.07 — 404).
  Его бэкенд `viewer-api` (`1nmUl22Iyns4kMK4NuzB7knzx-9Ly8DVqjpNzDs15cZUmZffPQAD2A4DD`)
  **отключён 31.07.2026**: анонимный деплой снят, адрес отдаёт 404. Скрипт остался в Drive
  пустой оболочкой, читать ему больше нечем. ⚠️ Это НЕ «Планировщик — просмотр»
  (`1pSvtsCBCN…`) — тот живой и нужен, его зовёт `planirovshik.web.app`. Два разных скрипта,
  я их однажды перепутал.
- Старый `scriptId` `1EF8J3JCPxVSKjLdjrUC_dFtUbfxXeZpkhauzRieePgVfkFy3zu2eNCwr` — привязан
  к удалённому дублю файла, заброшен.
- Netlify-версия формы удалена 08.07.2026. Рабочий адрес формы и Планировщика —
  **https://planirovshik.web.app** (Firebase Hosting).

### Прочее по проекту
- Оба файла «Планировщик расходов» в Drive одноимённые (старый `12zlcXPzG...` — архив,
  новый `1CYDwiM8...` — рабочий) — различать по ID/папке «EL NATUREL».
- **Локаль новой таблицы — русская: разделитель аргументов в формулах `;`, не `,`**
  (иначе `Range.setFormula()` даёт `#ERROR!` молча по цепочке).
- Script Properties скрипта планировщика (PARTNER_TOKEN/COMPANY_ID/YC_LOGIN/YC_PASSWORD/
  ADMIN_KEY) — свои, изолированы от журнального скрипта.
- Работаем и пушим в `main` этого репозитория напрямую. Ход работы, что сделано/осталось —
  в `STATUS.md` репозитория `Yclients_integration_tables_application`.



## Интеграция Тильда ↔ YClients (магазин) — замена МойСклада

Отдельный изолированный проект (не в куче с финансами). Источник правды — **YClients**;
Тильда получает остатки и цены, отдаёт заказы. Заведено 23.07.2026, оба потока на автопилоте
с 25.07.2026. Живой статус, грабли и проверки — в `STATUS.md` репозитория
`tilda-yclients-integracia-magazin`, не здесь.

### Доступы и адреса
- Репозиторий: `tilda-yclients-integracia-magazin` (приватный). Код в `main`:
  `apps-script/Code.js` + `appsscript.json` + `.clasp.json`.
  Обновление: `cd apps-script && clasp push`, затем `clasp deploy -i <deploymentId>`.
  ⚠️ Голый `clasp deploy` создаёт НОВЫЙ URL — тогда менять адрес вебхука в Тильде.
  ⚠️ `push` ВСЕГДА перед `deploy`: один раз deploy собрался из старого кода.
- Apps Script scriptId `1-4nrE2WsR9tpD0EyXtqsxdnTCI_xP1ZQNrC2eGuJP27DEqnIjesWP87K`
- Deploy web app = **адрес вебхука для Тильды**:
  https://script.google.com/macros/s/AKfycbzsC52qzjWtmUFITelkaCavpUd2mRiiD5I6XKMYXrgsMXEy3V75rZ3VD3NjDrXlS6V1Zw/exec
- Журнал-таблица (лист «Статус автоматики»):
  https://docs.google.com/spreadsheets/d/1MNi0B3NWL7GUT-8wyj0m14zpaRg2iNNwdexm357QWm8/edit
- YClients: company **328251**, склад магазина **637823** (`for_sale=1`); расходники 637822
  НЕ трогаем. Мастер операций — Ирина **969478** (YClients требует реального сотрудника).
  Токены — своя КОПИЯ ключей в Script Properties этого проекта (изоляция от финансов).
- Сайт: **https://elnaturelshop.ru** (Тильда). Коннектор CommerceML — один на проект,
  МойСклад от Тильды отключён. Письма об ошибках — `MailApp` + `OWNER_EMAIL_`.
- **Скил `my-skill-proverka-integracii`** (в репозитории проекта, `.claude/skills/`) — готовая
  процедура проверки здоровья интеграции. Применять при «всё ли в порядке?», после реальных
  заказов, после правок кода и в начале новой сессии — не выяснять состояние вручную заново.
- **Диагностика без риска:** два deployment-а — боевой (его URL в Тильде) и `@HEAD` со свежим
  кодом. Адрес `@HEAD` заканчивается на `/dev` (не `/exec`) и работает только с
  `Authorization: Bearer <access_token>`. Значит можно править код и проверять на боевых
  данных, не трогая вебхук.
- **Служебный `doGet` — только владелец** (закрыто 26.07, деплой v36). Пароля в коде нет:
  доступ по `Session.getActiveUser()` = `OWNER_EMAIL_`, всем остальным `forbidden`. Проверено
  запросами: анонимный вызов боевого `/exec` не проходит, вебхук Тильды отвечает 200 за 1,8 с.

### Каналы продаж — где что берётся (сводно, 29.07.2026)
| Канал | Остатки | Цены | Названия | Деньги в финансы |
|---|---|---|---|---|
| Магазин (YClients) | источник правды | источник правды | — | финоперации, статья 7 |
| Интернет-магазин (Тильда) | из YClients | из YClients | свои, не трогаем | проводка Ирины, статья «Интернет-магазин» 70466 |
| Ozon (FBS) | из YClients *(на автопилоте с 30.07.2026)* | **свои, НЕ синхронизируем** | **свои, НЕ синхронизируем** | проводка Ирины, статья «Ozon» 156010 |

### Как читать витрину сайта (для независимой сверки)
Статические страницы = кэш, врут; наружу отдаёт только фид:
`https://store.tildaapi.com/api/getproductslist/?storepartuid=<uid>&recid=<recid>&c=<любое>&getparts=true&getoptions=true&slice=1&size=300`
Пары `recid`/`storepartuid` — в HTML страницы категории (`recid:'…',storepart:'…'`; всего
по сайту 39 блоков). Пустой `storepartuid` не работает, списка категорий фид не отдаёт.
Полный список страниц — из `https://elnaturelshop.ru/sitemap.xml` (13 шт.), НЕ по ссылкам
с главной (так теряется `/sale`). В ответе у товара есть `externalid` — наш ключ.
⚠️ Выводы делать только когда ВСЕ запросы прошли: один упавший дал ошибочное «10 товаров нет
на сайте» вместо 2.

### Ключ сопоставления
**Tilda External ID = `_` + YClients `good_id`.** Совпал у всех 500 товаров → без дублей.
SKU в Тильде пуст — по артикулу матчить НЕЛЬЗЯ.

**Ozon `offer_id` — тот же ключ `_<good_id>`** (переведено 30.07.2026, 274 из 276 карточек:
«Прочее» 7, Tangle Teezer 68, Moroccanoil 94, Invisibobble 105). Два товара не сопоставлены
по решению Ирины: «Браслет Чан-Лу» (нет в YClients, удаляет из Ozon) и «Набор разноцветных
резинок с фигурками 10 шт» (товара нет, висит остатком). Дубликатов нет, каждый артикул
проверен по каталогу YClients. Смена — `POST /v1/product/update/offer-id`, ≤25 за раз;
после каждой партии перечитывать карточки: артикул встал И название не изменилось.
Связь с Яндекс.Маркетом не заводили.

⚠️ **Метод сопоставления по названиям** (пригодится при новых товарах). Обе стороны привести
к нижнему регистру, ё→е, **свести кириллические двойники латиницы** (`асеорхукмтвн`→`aceopxykmtbh`
— в названиях Ozon буквы перепутаны), отделить цифры от букв («250мл»=«250 мл»), русские слова
длиннее 5 букв обрезать до основы («заколки»=«заколка»). Мера Жаккара **со взвешиванием
редких слов** (IDF) — иначе название модели тонет среди общих «резинка для волос».
Раздавать кандидатов **глобально, один товар YClients — одной карточке Ozon** (жадно по
убыванию схожести): без этого две похожие карточки забирают один товар.
На этих правилах все 161 позиции второго этапа сошлись автоматически, спорных — 5.
- ⚠️ Категория **«Магазин»** в YClients = товары под маркетплейс (10 шт., 9 из них Moroccanoil
  мелких форматов 10–20 мл). Давать им перевес, но ТОЛЬКО когда кандидат и так почти лучший:
  безусловный приоритет притягивает чужое («CURL Control мусс 150 мл» → «FRIZZ CONTROL спрей 20 мл»).
- ⚠️ Ловушки, на которых ошибались: один оттенок в двух объёмах (200/30 мл), одна модель в
  трёх размерах (спрей 160/50/20 мл), «True Black» у пяти линеек сразу, похожие имена наборов
  (Petit Four / Petit Bijoux). Проверка на дубликаты артикулов ловит такое надёжнее сходства.

### Поток A: YClients → Тильда (остатки + цены)
- `GET /goods/{company}` (постранично) → остаток из `actual_amounts` по складу `for_sale`,
  цена `cost`. Заливка фидом **CommerceML** (`import.xml` + `offers.xml`), HTTP Basic.
- Остаток — **абсолютным числом, не разницей**: сверка встроена, ошибка не копится.
- Расписание: **5 мин** — изменения по складу; **каждый час** — полная сверка всех 500;
  раз в месяц — чистка журнала. Отдельного ночного прогона нет.
- Изменения ловим сравнением списка складских операций (`TX_SEEN`, окно 3 суток):
  появившиеся И исчезнувшие номера — исчезнувший = операцию удалили (`changed_after`
  у YClients НЕ работает). Ловится ЛЮБОЕ движение, не только продажи.
  ⚠️ **Изменение ЦЕНЫ движения не создаёт** → его видит только полная сверка (потому ежечасная).
- **Новые карточки Тильда создаёт сама** («создавать новые» ВКЛ) — название и цена из YClients,
  External ID проставляется, без категории; Ирина дозаполняет.
- **Названия и артикулы НЕ синхронизируем** — соответствующие галки в Тильде ВЫКЛ
  (проверено: прогон 500 товаров исправил 14 цен и не тронул ни одного названия).

### Поток B: Тильда → YClients (заказ с сайта → списание)
- Вебхук корзины (галки «товары массивом» + «externalid») → `doPost` → ОДИН
  `POST /storage_operations/operation` со всеми позициями (атомарно), **`type_id=1` продажа,
  `cost=0`** → финансовая операция НЕ создаётся (подтверждено на боевом), выручку Ирина
  проводит статьёй дохода. Функция `createOnlineSale_`.
- **Подпись** «Заказ из интернет-магазина El'Naturel Shop №<номер>» — двумя PUT после создания
  (позиции + документ). Не прошёл PUT → лист «Очередь повторов»; склад при этом уже верный.
  ⚠️ **Номер ведём САМИ** (`ORDER_SEQ_NEXT`, засеян 233): Тильда «порядковый номер заявки»
  в вебхук не передаёт — только внутренний `orderid`, `systranid` всегда 0. Разъехалось —
  `?action=setup&ORDER_SEQ_NEXT=<номер>`.
- Идемпотентность по `tranid`; лист несопоставленных для товаров без пары.
- ⚠️ **Подпись и нулевая цена — признак канала для ФИНАНСОВ** (связь двух проектов, 26.07.2026).
  Финансовая интеграция отличает продажу с сайта от магазинной так: цена позиции 0 → читает
  комментарий документа → ищет слова «интернет-магазин». Менять текст подписи или начать писать
  реальную цену — только вместе с финансовой интеграцией, иначе каналы перестанут разделяться.

### Нагрузка и лимиты (замерено 26.07)
- **YClients: 5 запросов в СЕКУНДУ** (официально). Мы делаем ≈650 в сутки, Тильда ≈200.
  Apps Script: 20 000 внешних запросов/сутки (у нас ~3%), 90 мин триггеров/сутки (~22 мин).
- ⚠️ Опасен не суточный объём, а ПИК. Реальные объёмы: заказ 1–10 позиций, ПРИХОД накладной
  10–150. Четыре уровня: (1) при >20 изменившихся товаров читаем постраничным каталогом
  (~8 запросов независимо от количества, `fetchGoodsSmart_`); (2) `ycPace_` — минимум 250 мс
  между обращениями (≤4 запроса/с); (3) `LockService` — пятиминутка и полная сверка никогда
  не идут параллельно; (4) повтор при 429 (3/9/20 с), у Тильды 10/20/30 с.
  5xx повторяем только для чтения — повтор POST продажи мог бы её задвоить.
  `doPost` замка не ждёт: Тильде нужен быстрый ответ.

### Грабли (проверено на боевом — не повторять)
- **Цена** применяется ТОЛЬКО с блоком `<ПакетПредложений>`+`<ТипыЦен>`. Без них ответ
  «success», а цена не меняется.
- ⚠️ **ВЕБХУК ТИЛЬДЫ АКТИВИРУЕТСЯ ТОЛЬКО ПРИ СОХРАНЕНИИ** и лишь если приёмник ответил 200
  за 5 секунд на проверочный `test=test`. Не успел — вебхук в списке есть, но не срабатывает
  НИКОГДА (потеряли полдня). Отвечать на `test=test` мгновенно, до таблиц/почты/API;
  после правок, влияющих на скорость, — ПЕРЕСОЗДАТЬ вебхук (пересохранения не хватило).
  Диагностика «дошло ли»: счётчики в `?action=master`; сравнить с Email/Telegram-приёмщиками
  того же блока — получили они заказ или нет.
- ⚠️ **Формат вебхука — form-urlencoded с КВАДРАТНЫМИ СКОБКАМИ** (`payment[products][0][…]`),
  `[quantity]` — количество, **`[amount]` — СУММА** (ловушка), `[systranid]` всегда 0.
- ⚠️ **YClients игнорирует `comment` при создании операции** — только отдельным PUT
  (документу нужны `type_id`, `storage_id`, `create_date` с полным временем; позиции — полное
  тело). `PATCH` — 405. **`GET /storages/transactions` комментарий позиции ВСЕГДА отдаёт
  пустым** — читать `GET /storage_operations/operation/{company}/{doc}`.
- ⚠️ **У CommerceML Тильды есть лимит частоты** («Many requests») → партии по 250, пауза 8 с,
  повтор `checkauth` 10/20/30 с.
- ⚠️ **У токена нет прав удалять складские документы** (405/403) — только руками Ирины.
- Вебхуков на склад у YClients НЕТ → только polling. Публичный API Тильды read-only,
  товары — только через CommerceML.

## Интеграция Ozon ↔ YClients (маркетплейс)

Отдельный изолированный проект. Источник правды — **YClients**; Ozon получает остатки.
Только **FBS**. Заведено 30.07.2026, поток A на автопилоте с того же дня. Живой статус, грабли
и порядок проверки — в `STATUS.md` репозитория `ozon-yclients-integracia`, не здесь.

### Доступы и адреса
- Репозиторий: `ozon-yclients-integracia` (приватный). Код в `main`: `apps-script/Code.js`.
  Обновление: `clasp push` из временной папки со своим `.clasp.json` (`rootDir` = абсолютный
  путь к `apps-script`; `rootDir: ""` ломает clasp 2.4.2).
- Apps Script scriptId `1XwW7qQpOriUtWhJ8gI7VvtIa_uGhO0DrZJ6ajVY8DofxqBSn_iPgdUpP`.
  Боевого деплоя НЕТ и не нужно — вебхуков не принимаем, Ozon опрашиваем сами.
  Диагностика — `@HEAD` `AKfycbxt4L7cyPSE1O1k0jt8-Y3XiIF_4rC0LWa8WF1pjypq` (`/dev`,
  только с `Authorization: Bearer`). Первый запуск требует разового ▶️ `authorize` от владельца.
- Журнал-таблица: https://docs.google.com/spreadsheets/d/1n6P2_VBztwAj11A0QYVMSMeydBgjn51hJiSEzEdsZuw/edit
- Ozon: Client-Id **751330**, склад FBS **«BLOOMES - магазин косметики»**, `warehouse_id`
  **1020000101490000**, Балашиха, Октябрьская 33. Ключ Seller API — только в Script Properties,
  истекает ~29.01.2027 (задача в Todoist на 22.01.2027, интеграция шлёт одно письмо за 7 дней).
- YClients: company **328251**, склад магазина **637823**. Своя КОПИЯ ключей (изоляция).
- **Скил `my-skill-proverka-ozon`** (в репозитории проекта) — готовая проверка здоровья,
  предлагать её самому.

### Рабочие методы Ozon (проверены живыми запросами 30.07.2026)
- `/v2/warehouse/list` — склады. Ответ в поле `warehouses`, НЕ `result`.
- `/v4/product/info/stocks` — остатки и заодно СПИСОК карточек:
  `{items:[{product_id, offer_id, stocks:[{type:"fbs", present, reserved, sku}]}], total, cursor}`,
  листать по `cursor`.
- `/v2/products/stocks` — запись остатков, 1–100 за раз, ответ `result[].updated/errors`.
- `/v3/product/list` — карточки с флагами `archived`, `has_fbs_stocks`; `/v3/product/info/list` —
  названия и `statuses` (по 100 `offer_id` за раз).
- `/v3/posting/fbs/list` — заказы FBS (понадобится потоку B).
- ⚠️ Устарели («obsolete method»): `/v1/warehouse/list`, `/v1/product/info/stocks-by-warehouse/fbs`.
  Версии проверять запросом, не по памяти. Отдельного метода «переименовать товар» НЕТ —
  название меняется только полной перезагрузкой карточки, поэтому правим руками.

### Поток A: YClients → Ozon (остатки)
- Пятиминутка — только изменившиеся товары (изменения ловим сравнением списков складских
  операций, как у Тильды). Полная сверка — раз в 3 часа в :35, читает остатки Ozon и шлёт
  ТОЛЬКО расхождения. Остаток — **абсолютным числом**.
- ⚠️ **Сверка ≠ обновление.** Остатки уезжают каждые 5 минут; сверка — подстраховка. Ежечасной
  её делать не надо: у Тильды она такая из-за ЦЕН, а цены в Ozon мы не синхронизируем.
- ⚠️ **Резерв Ozon не компенсируем намеренно** — занижение безопаснее перепродажи (штраф).
- ⚠️ **Пустой ответ Ozon по остаткам = отказ от сверки**, иначе одна кривая выдача обнулит витрину.
- ⚠️ **Общий источник каталога с интеграцией Тильды рассмотрен и ОТКЛОНЁН** (30.07): экономия
  ~7% запросов не окупает зависимость интеграций друг от друга и риск устаревших данных.
  Пик закрыт иначе: щадящий режим чтения (700 мс) + сверка втрое реже, в некруглые часы.

### Поток B: Ozon → YClients (заказ → списание) — работает с 30.07.2026
Схема ОДИН В ОДИН с Тильдой: продажа `type_id=1`, `cost=0` (финансовая операция не создаётся),
одна операция со всеми позициями, подпись двумя PUT, очередь повторов, идемпотентность.
Заказы забираем опросом `/v3/posting/fbs/list` каждые 5 минут, окно догона 14 дней.
- ⚠️ **Списываем в момент ЗАКАЗА, не отгрузки** — товар почти весь в одном экземпляре, и
  непроведённый заказ рискует быть проданным в магазине; за отмену Ozon штрафует.
  Возврат при отмене Ирина делает руками, интеграция шлёт напоминание письмом.
- ⚠️ **Подпись «Отгрузка №<номер отправления>»** — намеренно БЕЗ слова Ozon, чтобы сотрудники
  не видели канал (решение Ирины). Хранится в `ORDER_COMMENT_PREFIX`.
- ⚠️ Подпись + нулевая цена — по ним ФИНАНСЫ отличают канал. Слово «Отгрузка» общее, поэтому
  признак канала = СОЧЕТАНИЕ: цена 0 + «Отгрузка №» + номер вида `12345678-0001-1`.
  Финансовую интеграцию этому ещё НЕ научили — сделать при следующей работе с ней.

## Инвестиции (личный учёт Ирины: облигации, акции, купоны)

Google-таблица учёта личных инвестиций Ирины. Ирина присылает скрин сделки — ассистент
вносит данные в нужный лист, формулы считают всё сами (прибыль, комиссии, налог).
Заведено 22.07.2026.

**Навык `my-skill-obligacii-pravila-pokypki`** (`.claude/skills/` здесь же, в claude_md) —
правила ОЦЕНКИ облигации перед покупкой: чек-лист входа на отскоке, экономика сделки,
красные флаги. Применять САМОМУ, без напоминаний Ирины, всегда когда она присылает бумагу
на анализ или спрашивает «брать / не брать». Чек-лист — фильтр, не приговор: смотреть
каждую бумагу индивидуально и давать своё решение. Заведён 04.08.2026.

Комиссия Сбера: до 31.07.2026 — 0,3085% от суммы без НКД, **с 03.08.2026 — 0,315%**
(тариф сменился, проверено на трёх чеках). Ставку каждый раз сверять по чеку.

### Рабочая таблица (ЕДИНСТВЕННАЯ актуальная)
«Инвестиции — учёт» — https://docs.google.com/spreadsheets/d/1QU1Fb2iHEOeQ-EpuExAKgLn78i6FUxo_yJCANNiYU8c/edit
Папка «Инвестиции» в Drive Ирины, владелец — аккаунт Ирины. Файл **создан ассистентом**
(это важно — см. «Как пишем»).

### Структура — 4 листа (данные строк 9–208, одна строка = одна сделка)
- **Сводная** — собирает всё сама + налоги. Ставка НДФЛ в `Сводная!B27` = 13%, порог
  годовой базы 2.4 млн ₽, сверх — 15%. Руками только «Оплачено налога».
- **Облигации**. Руками: A Дата, B Инструмент, C Кол-во куплено, D Сумма покупки без НКД,
  E НКД покупка, H Комиссия покупки, I Кол-во продано, J Сумма продажи без НКД, K НКД продажа,
  N Комиссия продажи, W История. Формулы (не трогать): F,G,L,M,O,P,Q,R,S,T,U и
  **V Статус (авто, добавил ассистент)**.
- **Акции**. Руками: A Дата, B Инструмент, C Кол-во куплено, D Сумма покупки, F Комиссия
  покупки, G Кол-во продано, H Сумма продажи, J Комиссия продажи, S История.
  Формулы: E,I,K,L,M,N,O,P,Q и **R Статус (авто)**.
- **Купоны**. Руками: A Дата выплаты, B Инструмент/эмитент, C Сумма начисления, F ✓,
  G Комментарий (в самом конце — не мешает). Формулы: D Налог, E Чистый доход.
  Итоги в шапке (стр. 5): C5/D5/E5 = SUBTOTAL по C/D/E.

### Как пишем в таблицу (техническая механика — восстановить в новой сессии)
Google Sheets API НЕЛЬЗЯ (не включён в GCP-проекте clasp `1072944905499`, включить нельзя —
проект не наш). Пишем через **Drive API**, обновляя файл целиком, ссылка сохраняется:
- Доступ: публичный OAuth-клиент clasp (`client_id 1072944905499-vm2v2i5dvn0a0d2o4ca36i1vge8cvbn0`,
  секрет — в исходниках clasp), scope **`drive.file`**. Он покрывает ТОЛЬКО файлы, созданные
  этим приложением — поэтому рабочая таблица создана ассистентом (в ручную Ирины писать нельзя).
- Широкий scope `spreadsheets` этому клиенту Google ЗАПРЕЩАЕТ (блокирует экраном «приложение
  заблокировано») — повторно не пытаться.
- Запись: Drive export (→ .xlsx) текущей таблицы → правка `openpyxl` → `PATCH`
  `.../upload/drive/v3/files/{id}?uploadType=media` с .xlsx → перезапись в ту же ссылку.
  Формулы и форматирование сохраняются (проверено).
- Токен в репо НЕ хранится (безопасность). В КАЖДОЙ новой сессии — заново `clasp login
  --no-localhost` (дефолтные scope, не заблокированы): дать Ирине ссылку, она возвращает
  redirect-URL, обменять код (`curl oauth2.googleapis.com/token`), записать `~/.clasprc.json`.
