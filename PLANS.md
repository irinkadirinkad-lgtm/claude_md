# PLANS — планы и отложенные задачи (все проекты)

Зачем отдельный файл (решение Ирины 14.07.2026): идеи и договорённости «на потом» не должны
тонуть в STATUS конкретного репозитория — этот файл лежит в claude_md и грузится в КАЖДЫЙ чат,
поэтому планы видны из любого проекта. Правила ведения:
- Новая идея / отложенное решение → сразу сюда (🧠-пометка Ирине как обычно).
- Взяли в работу и сделали → отсюда убрать, факты перенести в PROJECT.md / STATUS.md проекта.
- Я сам периодически напоминаю Ирине, что здесь лежит, — не жду, пока спросит.
- ⏸ **Дубль в Todoist ПРИОСТАНОВЛЕН (решение Ирины 26.07)** — планы ведём только здесь.
  В подключённом аккаунте Todoist проекта «EL'NATUREL — планы» нет, только «Входящие»;
  задачи туда не создаём, пока Ирина не скажет иначе.

## Интеграция каналов продаж — замена МойСклад (YClients ↔ Тильда, потом Ozon)

Отдельный проект `tilda-yclients-integracia-magazin` (свой Apps Script, свои ключи).
Механизм, адреса и грабли — в разделе «Интеграция Тильда ↔ YClients (магазин)» файла
`PROJECT.md`; ход работы — в `STATUS.md` того репозитория. Здесь только план.

- ✅ **ОБА ПОТОКА РАБОТАЮТ на автопилоте (25–26.07).** Остатки и цены из YClients на сайт
  (5 мин + ежечасная полная сверка), заказы с сайта → списание в YClients с подписью
  «Заказ из интернет-магазина El'Naturel Shop №<номер заявки>». Полный цикл пройден настоящим
  заказом. Финоперации не создаются — выручка не задваивается. МойСклад больше не нужен.
- ⏭️ **Осталось:**
  • Обкатать на первых реальных онлайн-продажах — смотреть журнал и письма.
    Проверять скилом `my-skill-proverka-integracii` (в репозитории проекта) — не выяснять
    состояние вручную. Я сам предлагаю проверку, Ирина об этом не напоминает.
  • **Закрыть диагностический люк:** `doGet` открыт наружу и гейтится захардкоженным секретом
    (можно перезаписать Script Properties, создать операцию). Удалить `setup`/`delDoc`/`testOp`
    или перенести секрет в Script Property.
  • **Связка с финансами (Блок B) — ✅ СДЕЛАНО 26.07.** Интернет-магазин отделён от магазина:
    деньги из проводки Ирины, товар из складской операции сайта. ⚠️ Подпись «интернет-магазин»
    и нулевая цена позиции теперь несут финансовый смысл — менять только вместе с финансовой
    интеграцией.
  • **Ozon** — вторым этапом: своя интеграция остатков и цен (у Ozon свои цены и своя правка).
    Разделение канала в финансах уже сделано — интеграция должна лишь ставить пометку «Ozon»
    в комментарий складского списания, как это делает интеграция сайта.
    Позже — сузить права токена.
- ⚠️ **Сказать Ирине при первом отменённом заказе:** удаление заказа в ТИЛЬДЕ остаток в YClients
  НЕ возвращает (при МойСкладе возвращало). Сайт получает остаток ИЗ YClients абсолютным числом,
  правка на стороне Тильды затрётся. Возвращать надо в YClients: удалить складскую операцию
  «Продажа товара» по этому заказу (найти по подписи с номером заявки) — через ≤5 мин сайт
  обновится сам. Отменённые заказы Ирина ведёт вручную, автоматика возврата НЕ нужна
  (решение 25.07): она и так связывается с каждым покупателем по оплате.

## Прочее отложенное

- ⚠️ **Смена года 2027** — захардкоженные месяцы 2026 в матрицах (розовая таблица + приложение).
  Вернуться НЕ ПОЗЖЕ декабря 2026, иначе с января новый год сам не появится.
  ⚠️ Напоминание живёт ТОЛЬКО здесь (дубль в Todoist приостановлен) — поднять этот пункт
  самому в ноябре-декабре 2026, Ирина не напомнит.
