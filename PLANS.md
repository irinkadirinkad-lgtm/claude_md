# PLANS — планы и отложенные задачи (все проекты)

Зачем отдельный файл (решение Ирины 14.07.2026): идеи и договорённости «на потом» не должны
тонуть в STATUS конкретного репозитория — этот файл лежит в claude_md и грузится в КАЖДЫЙ чат,
поэтому планы видны из любого проекта. Правила ведения:
- Новая идея / отложенное решение → сразу сюда (🧠-пометка Ирине как обычно).
- Взяли в работу и сделали → отсюда убрать, факты перенести в PROJECT.md / STATUS.md проекта.
- Я сам периодически напоминаю Ирине, что здесь лежит, — не жду, пока спросит.
- Дубль крупных планов — в Todoist (проект «EL'NATUREL — планы»), чтобы у Ирины было на виду;
  веду синхронно: добавила план — добавила задачу, сделали — закрыла.

## Yclients Finance App — Блок B: онлайн-каналы (ИМ + маркетплейсы)

Старт — по команде Ирины. Состав:

- **Разнесение себестоимости по каналам.** ✅ РЕШЕНИЕ Ирины (14.07): различаем по
  **комментарию складской операции списания**: «маркетплейс» / «озон» → МП;
  «интернет-магазин» / «сайт» → ИМ; списание, связанное с продажей через кассу салона
  (или без комментария) → магазин. Парсер гибкий по ключевым словам, регистр неважен.
  Ирина при списании ИМ/МП-заказа пишет слово в комментарий — это её постоянное действие.
  (Отклонены: отдельный склад «Маркетплейс» — дубляж товаров при едином складе;
  автоматч по дню/сумме — хрупко, списание и приход денег расходятся на дни.)
- **Комиссия ИМ по кассе** (карта 3,05% / QR 0,7% / нал 0 — нал Ирина отключает):
  механизм `acquiringRate_` готов; при ПЕРВОЙ реальной операции ИМ разбить статью по кассам.
- **Себестоимость в чистой прибыли ИМ и МП** (сейчас 0, вся сидит в магазине; ОБЩИЕ цифры честные).
- **Кол-во продаж и средний чек по каналам** (сейчас общее по складу — в «Магазине» завышено на
  МП-штуки). Строки «Кол-во продаж» и «Средний чек» в под-вкладках ИМ/МП приложения уже стоят
  нулями (14.07) — Блок B наполнит реальными.
- **Бренды по каналам**: `imBrands`/`mpBrands` в app-api и приложении (таблицы-нули уже готовы).
- Бэкфилл МП — не задваивать статью (заметка со сверки).

_Детальная себестоимость услуг (по услуге, не по клиенту) — СДЕЛАНО 18.07, факты в PROJECT.md / STATUS.md._

## Интеграция каналов продаж — замена МойСклад (YClients ↔ Тильда, потом Ozon)

Убираем МойСклад — наша интеграция сама становится «связочным звеном». Этапами: сначала Тильда,
потом Ozon. Отдельный проект `tilda-yclients-integracia-magazin` (свой Apps Script). Детали, адреса,
доступы, грабли — в его `STATUS.md`.

- ✅ **Поток A (магазин → сайт: остатки + цены) — РАБОТАЕТ на автопилоте (25.07):** инкремент 5 мин
  (только изменения) + ночная полная сверка + чистка журнала. Первый полный прогон 500 сделан,
  14 устаревших цен на сайте исправлены. Названия НЕ синхроним (в Тильде — свои продающие,
  в YClients — по прайсам поставщиков). Ключ = `_`+good_id (без дублей). Нагрузка минимальна.
- ⏭️ **Осталось:**
  • **Поток B вживую** (заказ с сайта → «продажа» 0 ₽ в YClients, без финоперации): механизм готов и
    проверен, но не подключён. Вставить наш `…/exec` в Тильде (Настройки сайта → Формы → Webhook,
    галки «товары массивом» + «externalid»), снять поля тестовым заказом, включить.
  • Обкатать инкремент на первой реальной онлайн-продаже.
  • **Ozon** — вторым этапом (свои цены + правка цен отдельно).
  • Связка с финансами (Блок B): онлайн-продажи попадут в кол-во товара «Без сотрудника — продажи»,
    выручка = 0 (её Ирина проводит статьёй) → свести, чтобы не задвоилось.
  • Залить код в GitHub (`add_repo` в сессии не подключался — код в Apps Script + локально).

## Прочее отложенное

- ⚠️ **Смена года 2027** — захардкоженные месяцы 2026 в матрицах (розовая таблица + приложение).
  Вернуться НЕ ПОЗЖЕ декабря 2026, иначе с января новый год сам не появится. (Задача в Todoist
  с датой.)
