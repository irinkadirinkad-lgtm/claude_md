# PLANS — планы и отложенные задачи (все проекты)

Зачем отдельный файл (решение Ирины 14.07.2026): идеи и договорённости «на потом» не должны
тонуть в STATUS конкретного репозитория — этот файл лежит в claude_md и грузится в КАЖДЫЙ чат,
поэтому планы видны из любого проекта. Правила ведения:
- Новая идея / отложенное решение → сразу сюда (🧠-пометка Ирине как обычно).
- Взяли в работу и сделали → отсюда убрать, факты перенести в PROJECT.md / STATUS.md проекта.
- Я сам периодически напоминаю Ирине, что здесь лежит, — не жду, пока спросит.
- **Планы ведём только здесь** (решение Ирины 30.07): проект «EL'NATUREL — планы» в Todoist
  закрыт как завершённый, дубля планов там больше нет. В Todoist заводим отдельные задачи
  по необходимости, во «Входящие», с уже существующими метками.

## Интеграция каналов продаж — замена МойСклад (YClients ↔ Тильда, потом Ozon)

Отдельный проект `tilda-yclients-integracia-magazin` (свой Apps Script, свои ключи).
Механизм, адреса и грабли — в разделе «Интеграция Тильда ↔ YClients (магазин)» файла
`PROJECT.md`; ход работы — в `STATUS.md` того репозитория. Здесь только план.

- ✅ **ОБА ПОТОКА РАБОТАЮТ на автопилоте (25–26.07).** Остатки и цены из YClients на сайт
  (5 мин + ежечасная полная сверка), заказы с сайта → списание в YClients с подписью
  «Заказ из интернет-магазина El'Naturel Shop №<номер заявки>». Полный цикл пройден настоящим
  заказом. Финоперации не создаются — выручка не задваивается. МойСклад больше не нужен.
- ⏭️ **Осталось:**
  • Обкатать на первых реальных онлайн-продажах — смотреть журнал и письма.
    Проверять скилом `my-skill-proverka-integracii` (в репозитории проекта) — не выяснять
    состояние вручную. Я сам предлагаю проверку, Ирина об этом не напоминает.
  • **Закрыть диагностический люк:** `doGet` открыт наружу и гейтится захардкоженным секретом
    (можно перезаписать Script Properties, создать операцию). Удалить `setup`/`delDoc`/`testOp`
    или перенести секрет в Script Property.
  • **Связка с финансами (Блок B) — ✅ СДЕЛАНО 26.07.** Интернет-магазин отделён от магазина:
    деньги из проводки Ирины, товар из складской операции сайта. ⚠️ Подпись «интернет-магазин»
    и нулевая цена позиции теперь несут финансовый смысл — менять только вместе с финансовой
    интеграцией.
  • **Ozon** — вторым этапом: своя интеграция (см. отдельный раздел ниже).
    Разделение канала в финансах уже сделано — интеграция должна лишь ставить пометку «Ozon»
    в комментарий складского списания, как это делает интеграция сайта.
    Позже — сузить права токена YClients.
- ⚠️ **Сказать Ирине при первом отменённом заказе:** удаление заказа в ТИЛЬДЕ остаток в YClients
  НЕ возвращает (при МойСкладе возвращало). Сайт получает остаток ИЗ YClients абсолютным числом,
  правка на стороне Тильды затрётся. Возвращать надо в YClients: удалить складскую операцию
  «Продажа товара» по этому заказу (найти по подписи с номером заявки) — через ≤5 мин сайт
  обновится сам. Отменённые заказы Ирина ведёт вручную, автоматика возврата НЕ нужна
  (решение 25.07): она и так связывается с каждым покупателем по оплате.

## Интеграция Ozon — В РАБОТЕ (начато 29.07.2026)

**Решения Ирины (не переспрашивать):**
- Торгуем **только FBS** (свой склад «BLOOMES», Балашиха, Октябрьская 33). FBO — разовая
  случайность Ozon, не наш канал.
- **Цены и названия в Ozon НЕ синхронизируем** — она считает и пишет их сама. Нужны только
  **остатки YClients → Ozon** и **заказы Ozon → списание в YClients** с пометкой «Ozon»
  (пометку финансы уже понимают), с нулевой ценой — как с Тильдой. Выручку канала она проводит
  статьёй дохода «Ozon» сама; из неё считаем налог и себестоимость, комиссию площадки НЕ считаем.
- ⚠️ **Порядок работы, которого Ирина требует строго:** сопоставление показываем ГРУППАМИ,
  ПОЛНЫМИ НАЗВАНИЯМИ с обеих сторон (без артикулов — они ей ни о чём не говорят), включая
  бесспорные. Она подтверждает — только тогда меняем. По каждой группе после смены —
  перепроверка, что артикул встал и НАЗВАНИЕ не изменилось.

**Доступ:** Client-Id `751330`, ключ Seller API — в переписке (в репозиторий не кладём).
⚠️ Ключ живёт 6 месяцев: создан 29.07.2026, истекает ~29.01.2027. Задача в Todoist заведена
(«Обновить ключ Ozon Seller API», 22.01.2027, приоритет 1, метка «Работа»). Будущая интеграция
должна дублировать это письмом за неделю и ловить отказ авторизации.

**Проверенные возможности API** (пустым списком, ничего не меняя): смена артикулов
`/v1/product/update/offer-id` — 25 за раз; цены `/v1/product/import/prices` — 1000;
создание товаров `/v3/product/import` — 1000; остатки `/v2/products/stocks` — 100.
Отдельного метода «переименовать товар» НЕТ (`/v1/product/update` и `/update/attributes` — 404):
название меняется только полной перезагрузкой карточки, поэтому правим руками.

**Артикулы сведены — этап закрыт 30.07.2026.** 274 из 276 карточек Ozon переведены на
`_<good_id>`, два товара не сопоставляем по решению Ирины. Метод сопоставления и его грабли —
в PROJECT.md, раздел «Ключ сопоставления»; здесь не дублируем.

**Рабочие методы Ozon** (проверены запросами 30.07, ничего не меняя):
`/v2/warehouse/list` — склад FBS **«BLOOMES - магазин косметики»**, `warehouse_id`
`1020000101490000`, Балашиха, Октябрьская 33. `/v4/product/info/stocks` — текущие остатки
с нашими `offer_id`. `/v2/products/stocks` — запись остатков (1–100 за раз).
`/v3/posting/fbs/list` — заказы FBS (за июль пусто).
⚠️ Устарели и отвечают «obsolete method»: `/v1/warehouse/list`,
`/v1/product/info/stocks-by-warehouse/fbs`. Версии методов проверять запросом, не по памяти.

**Что осталось построить** (сама интеграция, ещё не начата):
1. Остатки YClients → Ozon (`/v2/products/stocks`, по 100). Склад FBS «BLOOMES».
2. Заказы Ozon → списание в YClients с пометкой «Ozon» и нулевой ценой — как с Тильдой.
   ⚠️ Подпись канала в комментарии складского документа + нулевая цена: по ним финансы
   отличают канал, менять только вместе с финансовой интеграцией.
3. Своя песочница: отдельный Apps Script проект, свои Script Properties и триггеры.
   Репозиторий — `ozon-yclients-integracia` (решено 30.07; создаёт Ирина, у меня нет прав).
4. Ключ живёт до ~29.01.2027 — интеграция должна предупредить письмом за неделю и поймать
   отказ авторизации.

**За Ириной:** поправить 6 названий в кабинете Ozon — перепутаны русские и латинские буквы
(список отправлен письмом 29.07). На артикулы не влияет, сопоставление их пережило.
⚠️ Проверить карточку «HAIR TIE EARTH Black»: в Ozon названа «резинка» (штука), в YClients
это упаковка 5 шт. Если продаётся поштучно — остатки поедут, когда включим синхронизацию.

## Прочее отложенное

- ⚠️ **Смена года 2027** — захардкоженные месяцы 2026 в матрицах (розовая таблица + приложение).
  Вернуться НЕ ПОЗЖЕ декабря 2026, иначе с января новый год сам не появится.
  ⚠️ Напоминание живёт ТОЛЬКО здесь — поднять этот пункт самому в ноябре-декабре 2026,
  Ирина не напомнит.
